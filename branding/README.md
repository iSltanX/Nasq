# هوية نَسَق البصرية — «مخطوطة رقمية فاتحة»

## الفكرة

النص على الورق المسطَّر الفاتح: خطوط بطانة ذهبية خافتة وخط هامش، وكلمة «نسق»
بالحبر الداكن، والنقطة الحُمرة في أعلى الهامش علامة المُصحِّح الصامت.
الهوية الوحيدة التي تحمل غرفتَي التطبيق معًا: الناسخ يكتب بالحبر ويُذهِّب البطانة
(نَسَق)، والمُصحِّح يعلِّم بالحُمرة (شَذْب) — «البوابة الأخيرة قبل النشر».

اعتُمدت في v5.2.0 من أربعة اتجاهات صُمِّمت في Figma («الاتجاه 4: مخطوطة رقمية»)،
ثم قُلبت إلى **الفاتح** في v6.0.0 (2026-07-09) مع اعتماد الهوية الفاتحة أساسًا
للتطبيق — الفكرة نفسها معكوسة: ورق فاتح وحبر داكن بدل الحبر الليلي.

## الألوان

لوحة التطبيق الفاتحة نفسها (ثيم v6.0) — الهوية والواجهة من معجم واحد:

| الاسم | القيمة | الاستخدام |
|---|---|---|
| ورق دافئ | `#F4EFE6` | خلفية الجسم/الأيقونة |
| حبر داكن | `#1C1710` | كلمة «نسق» والمتن |
| ذهب هادئ | `#9C7724` | خطوط البطانة والهامش والحدود والأزرار الرئيسة |
| حُمرة المُصحِّح | `#B3583A` | نقطة المُصحِّح وأزرار شَذْب |

الشفافيات الدقيقة (بطانة 0.16، هامش 0.32، نقطة 0.85) — مُعمّقة قليلًا لتُقرأ على الورق الفاتح.

## نسخة ممتلئة (full-bleed) — لا أبيض

الأيقونة **تملأ المربّع كاملًا حتى الحواف** بزوايا مستديرة: لا هامش، لا حدّ، لا ظل.
هوامش آبل القديمة (جسم 824 داخل 1024 مع محيط شفّاف) كانت تجعل macOS يضع خلفها
**صفيحة بيضاء** تلقائيًا — أُزيلت نهائيًا. نسخة واحدة تُشتقّ منها كل الأحجام.

## الملفات

- `nasaq-appicon.svg` — الأيقونة الممتلئة (1024، الحبر يملأ المربّع، زوايا `rx=220`، بلا ظل ولا حدّ) — النسخة المعتمدة
- `nasaq-appicon-small.svg` — بديل اختياري للأحجام الصغيرة (كلمة أكبر، بلا بطانة) — ممتلئ وبلا أبيض أيضًا، غير مستخدَم في الطقم الحالي (نسخة واحدة)
- `nasaq-mark.svg` — الرمز وحده (بطاقة 32) بلا ظل، للواجهات والترويسات — نسخة مطابقة في `src/nasaq-mark.svg`
- `nasaq-logo.svg` — الشعار الأفقي (400×88): الوردمارك «نَسَق» على الورق المسطّر
- `Nasaq.icns` — ملف الأيقونة الجاهز لتطبيق macOS (كل الخانات من النسخة الممتلئة الواحدة)
- `AppIcon.iconset/` — مصدر الأحجام العشرة بأسماء آبل القياسية
- `png/` — تصديرات PNG (كلها من النسخة الممتلئة الواحدة)

## ملاحظات استخدام

- كلمتا «نسق» (وزن 900 في الأيقونة والرمز) و«نَسَق» (وزن 800 في الشعار)
  **مسارات SVG مرسومة**، مستخرجة من خط **Cairo** المتغيّر (Google Fonts،
  رخصة OFL — خط واجهة التطبيق نفسه) بتشكيل عربي صحيح عبر HarfBuzz،
  فلا تعتمد على خطوط النظام وتظهر متطابقة في كل العارضات.
- الهوية مصمَّمة لخلفية داكنة. على خلفية فاتحة استخدم `#7A5418` للكلمة
  والخطوط، و`#12100C` للنسخة الأحادية.
- لا تضف توهجًا أو ظلالًا على الرمز داخل الواجهة؛ `nasaq-mark.svg` مسطّح عمدًا.
- عند إعادة توليد الأيقونات: `npx tauri icon ./icon-source.png` يولّد الأطقم
  كلها من `icon-source.png` (النسخة الممتلئة الواحدة). لا حاجة لأي تبديل مقاسات.
- **تنبيه عند إعادة تصيير `icon-source.png` من SVG**: `qlmanage` يملأ المناطق
  الشفّافة بالأبيض، فيخبز أبيضَ في الزوايا المستديرة. الصحيح: صيّر نسخةً
  **مربّعة مصمتة** (`rx=0`، بلا شفافية) ثم افرض قناع الزوايا المستديرة بألفا
  حقيقية عبر Pillow (`rounded_rectangle` → `putalpha`) — فتصير الزوايا شفّافة
  فعلًا لا بيضاء. زوايا الناتج يجب أن تكون `alpha=0` والمركز داكنًا معتمًا.
