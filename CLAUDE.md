# NSQ — دليل المشروع

تطبيق سطح مكتب واحد (Tauri) يضمّ نظامين وظيفيين معزولين:

- **نسق** — برج التنسيق (نداءات النموذج: `format_text`, `generate_variation`, `adjust_lines`).
- **شذب** — برج شَذْب (أمر: `prune_text`؛ مخرجه بطاقة قراءة وقائمة قصّات محقّقة آليًا).

## قاعدة العزل بين نسق وشذب (ملزمة)
- التطبيق واحد، والبرجان معزولان عمدًا.
- لا تُدمج البرومبتات ولا العقود ولا منطق المخرجات بين البرجين.
- لا استيراد متبادل بين `nasaq/` و `shadhb/`؛ ولكل برج أن يعتمد على الخدمات العامة في `shared/`، مع منع وضع أي prompt أو عقد أو قاعدة سلوك نموذجية في `shared/`.
- لا ينتقل تعديل من برج إلى الآخر بلا طلب صريح، ولا نقل برومبت أو ثابت أو منطق بينهما ولو تطابقت الصياغة حرفيًا.
- المشترك الحقيقي فقط (نقل خام للمزود، تخزين محلي، حافظة) يوضع في `shared/`.
- يجب أن يبقى `tests/isolation.test.js` أخضر بعد أي تعديل.

## الحرّاس
- عقود وبرومبتات كل برج يحرسها وكيله: `.claude/agents/nasaq-prompt-guardian.md` و `.claude/agents/shadhb-prompt-guardian.md`.
- المراجعة العامة والتصحيح والاختبار والإصدار من الطاقم العام على مستوى المستخدم (`~/.claude/agents/`).
