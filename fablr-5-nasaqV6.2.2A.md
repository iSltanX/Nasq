# تقرير تدقيق شامل — نَسَق v6.1.0

**المدقِّق:** Claude Fable 5 — جلسة تدقيق آلي شامل (قراءة كود + اختبارات + معاينة حيّة)
**التاريخ:** 2026-07-10
**النسخة المفحوصة:** v6.1.0 — الفرع `main` عند `1ef7267` (شجرة عمل نظيفة)

**نطاق الفحص:** قراءة كاملة للكود (Rust + JS + CSS + HTML)، تشغيل الاختبارات (Node ‏90/90 ✅، Rust ‏48/48 ✅)، فحص التبعيات (`cargo audit` + `npm audit`)، ومعاينة بصرية حيّة في المتصفح (الوضعان، الثيمات، الوضع الليلي، النافذة الضيقة، حقن نتائج فحص محلية عبر واجهة التشخيص الموثّقة). **قيد معلوم:** المعاينة في WebKit المتصفح لا داخل Tauri نفسه، فمسارات النموذج الفعلية وpbcopy وتغيّر DPI بين شاشات لم تُختبر تشغيلًا — فُحصت كودًا فقط. **لم يُعدَّل أي ملف.**

---

## ١) الواجهة / البصري / RTL

**١-أ (متوسطة) — اختصار ⌘+Enter يعمل داخل وضع شَذْب ويطلق نداء نسق خفيًا**
`src/nasaq.js:342` — مستمع ⌘+Enter يفحص `formatBtn.disabled` فقط، والزر في وضع شَذْب مخفي (`display:none`) لا معطَّل. **التكرار:** بدّل إلى شَذْب، اضغط ⌘+Enter — تحقّقت منه حيًّا: `format_text` يُستدعى فعلًا، والنتيجة والمؤشر الدوّار يُكتبان في لوحة مخفية. داخل التطبيق هذا نداء Gemini حقيقي يُدفع ثمنه دون أي أثر مرئي. **المقترح:** حارس في المستمع — لا تنفيذ إن كان `data-mode="shadhb"` على الجذر (تعديل في nasaq.js وحده، لا مساس بشَذْب).

**١-ب (منخفضة) — تفاوت ارتفاع «نسق» عن القائمتين المجاورتين**
`src/styles.css:280` — الزر الرئيس 30px والقوائم 40.5px في الصف نفسه (قِيس حيًّا)، والتعليق يعِد بأنه «بارتفاع قريب من القوائم». **المقترح:** توحيد الحشوة الرأسية أو `min-height` مشترك.

**١-ج (منخفضة) — لا أنماط `:focus-visible` في الملف كله**
`src/styles.css:498` — صفر قواعد `focus-visible` (فحص برمجي)، وحقول النص تلغي `outline` وتكتفي بتغيّر حدّ خافت (0.24 شفافية). أزرار لوحة المفاتيح تعتمد على حلقة WebKit الافتراضية الباهتة. **المقترح:** قاعدة `:focus-visible` واحدة عامة بلون المعدن.

**١-د (منخفضة) — عدّاد الأحرف يعدّ وحدات UTF-16 لا محارف**
`src/shell.js:18` — إيموجي واحد يُعرض «2 حرفًا» (تحقّق حيّ). للعربية والتشكيل العدّ صادق، وينسحب الأمر على حدود المنصات (٢-هـ أدناه).

**١-هـ (منخفضة) — تعليقات الهيكل ترقّم ميزات v6.1.0 بأرقام v6.2 وv6.3**
`src/index.html:25` و`src/index.html:47` — «توأما الرمز (v6.3)» و«الوضع الليلي (v6.2)» بينما سجلّ الإصدارات ينسبهما إلى v6.1.0. لبس مستقبلي عند الرجوع للتاريخ.

**١-و (منخفضة) — تباين عنوان «نَسَق» الذهبي في الوضع الفاتح**
`src/styles.css:221` — `--brand` على الورق ‏3.61:1 لنص 16px عريض (العتبة 4.5). عنصر هوية لا محتوى، لكنه دون AA. في الليلي ممتاز (8.14).

**١-ز (منخفضة) — منظومة الفجوات شبه متسقة مع استثناء واحد**
قياسات حيّة: صفوف أدوات النتيجة 6px، شريط المسودات وأزرار المسودة 8px، الرأس 10px، الشريط العلوي 12px، صف المحاور 14px. التدرّج مقصود ظاهرًا، لكن صفّي أدوات متكافئين وظيفيًا (أدوات النتيجة 6px مقابل أدوات المسودات 8px) يفترقان بلا مسوّغ.

**سليم بعد الفحص:** اتجاه RTL في كل اللوحات، انعزال العدّادات والنسخة LTR (`unicode-bidi`)، علاج مؤشر WebKit (`empty-bidi`) يعمل، لا وميض ثيم عند الإقلاع (السكربت المضمّن قبل CSS)، الخطوط محلية بسلاسل احتياط (Geeza Pro / system-ui)، شريط التمرير يسارًا، النافذة الضيقة (800×560 — الحد الأدنى) تتكدس عمودًا واحدًا سليمًا وإن ضاقت مساحة النتيجة تحت الطية، Escape يغلق اللوحات بالأولوية الصحيحة (اختُبر)، لا أخطاء console إطلاقًا، ولا معالجة كشيدة أصلًا — وتركُها كما كتبها صاحبها هو الصواب لعهد الأداة.

---

## ٢) الوظيفي / المنطقي

**٢-أ (متوسطة) — سباق بين «نسق» و«سطور أقل/أكثر»**
`src/nasaq.js:294` و`src/nasaq.js:465` — `formatText` يعطّل زره فقط، و`adjustLines` يعطّل زرّيه فقط، وطبقة التحميل داخل صندوق النتيجة لا تحجب بقية الأزرار. **التكرار:** اضغط «سطور أكثر» (نداء بطيء) ثم «نسق» — نداءان متوازيان يكتبان على النتيجة بالتناوب، وآخر مستجيب يفوز، وذاكرة الجلسة تسجّل الاثنين. **المقترح:** علم انشغال واحد مشترك للنداءات الثلاثة.

**٢-ب (منخفضة) — «فصل الجمل» لا يفصل إلا عند النقطة**
`src/lines.js:79` — تلميح الزر يقول «كسر سطر بعد كل جملة»، والدالة تعالج «.» فقط. تحقّق حيّ: «نام الطفل؟ صرخ الديك!» بقيا موصولين. **المقترح:** إمّا ضمّ «؟ ! …» لمحارف الفصل أو تدقيق نص التلميح.

**٢-ج (منخفضة) — رمز 402 (نفاد الرصيد — العلّة المعروفة سابقًا) بلا رسالة مخصصة**
`src-tauri/src/shared/llm.rs:163` — الأذرع تغطي 401/403 و404 و429 و5xx، و402 يسقط في الذراع العام «فشل الطلب (رمز 402)». **المقترح:** ذراع صريح: «نفد رصيد المزود — اشحن الحساب».

**٢-د (منخفضة) — استيراد المسودات فوق السقف يقتطع المستورَد بصمت**
`src/shell.js:763` — الأمّهات المستوردة تُلحق في **ذيل** القائمة، ثم `drafts.length = DRAFTS_MAX` يقص الذيل نفسه، والرسالة تعلن العدد المحسوب قبل القص. لا يظهر إلا فوق 100 أمّ. **المقترح:** حساب `added` بعد القص أو تنبيه عند بلوغ السقف.

**٢-هـ (منخفضة) — حدود المنصات تقريبية كما هو موثَّق، مع ملاحظتين**
`src/nasaq.js:554` — لإكس: العربية والتشكيل يُحسبان محرفًا محرفًا وهذا **مطابق فعلًا** لقواعد إكس (نطاق 0-4351 وزنه 1)، والإيموجي بوحدتين يطابق وزن إكس 2 مصادفةً سعيدة — العدّ لإكس أدق مما يعِد التعليق. لثريدز وإنستغرام: الإيموجي يُحسب مضاعفًا فيُنذر مبكرًا — الاتجاه الآمن. سليم عمليًا؛ للعلم فقط.

**٢-و (منخفضة) — قيمة المنصة القديمة «سابستاك» تفقد أدواتها عند الاستعادة**
`src/nasaq.js:87` مقابل `src-tauri/src/nasaq/contracts.rs:21` — الخلفية تعامل الاسم القديم كمقال سابستاك، أما `isSubstackFace` الأمامية فلا تعرفه: استعادة مسودة قديمة بهذه القيمة لا تُظهر «تصدير لسابستاك» ولا بطاقة الشذرة رغم أن الصيغة سابستاكية.

**٢-ز (منخفضة) — drafts.json التالف يُستبدل نهائيًا عند أول حفظ**
`src-tauri/src/shared/drafts.rs:19` — التلف يعيد قائمة فارغة (قرار موثَّق كي لا تتعطل اللوحة)، لكن أول حفظ بعدها يكتب فوق الملف التالف فيضيع ما كان قابلًا للإنقاذ يدويًا. **المقترح:** قبل الكتابة فوق ملف تعذّر تفكيكه، نسخه إلى `drafts.json.corrupt`.

**٢-ح (معلومة) — عدّاد المحاولات لا يُفرَّغ**
`src/nasaq.js:196` — `attemptCounts` تنمو بعدد النصوص المجرَّبة في الجلسة. أثر ذاكرة زهيد؛ للعلم.

**سليم بعد الفحص (اختبار حيّ حيث أمكن):** «تنظيف فقط» محلي ويعالج العلامات اللاتينية بعد العربية ويحمي 3.14 والروابط؛ التراجع عند الصفر يعتذر بلطف، والسقف 20 يزيح الأقدم، وبدء نص جديد يصفّره، ولقطاته محصّنة ضد نصٍّ أجنبي (حارس المفتاح المطبَّع)؛ التبديل بين الوضعين لا يسرّب حالة، وحارس الجسر يرفض الإرسال إن تغيّرت الخانة بعد الفحص؛ فارغ/فراغات فقط يعتذران؛ النص الأطول من السقف يعود برسالة «قسّمه» الواضحة (فحص `finish_reason`)؛ إعادة المحاولة عند 400/422 تُسقط `response_format` **ولا تُسقط أبدًا** ضبط التفكير؛ المسودات: العرض الهرمي والبحث والحذف بخطوتين والدمج بلا تكرار كلها تعمل (اختبارات + معاينة)، والصيغة القديمة المسطّحة تُرحَّل.

**قفل thinking_budget=0 (البند الحساس):** مؤكَّد أنه **ما زال مفروضًا في كل مسار** — ثابت صلب `src-tauri/src/nasaq/contracts.rs:38` تمرّره النداءات الثلاثة جميعًا، ويُعاد بناؤه في جسم **كل** محاولة إعادة `src-tauri/src/shared/llm.rs:147`، ويغطي Gemini المباشر وOpenRouter (حقل `reasoning`) معًا، ولا مسار واجهة أو إعدادات يرفعه، وستة اختبارات تسيّجه. لا ملاحظات.

---

## ٣) تحت الغطاء / الأمن

**٣-أ (متوسطة) — CSP معطَّلة كليًا**
`src-tauri/tauri.conf.json:21` — `"csp": null`. لا ثغرة فعلية اليوم (كل مخرجات النموذج تمر عبر `textContent` حصرًا — تحقّق بحقن `<img onerror>` في مسودة ولم ينفَّذ)، لكن غيابها يجعل أي زلّة `innerHTML` مستقبلية قابلة للاستغلال فورًا مع `withGlobalTauri` مفعَّلة. **المقترح:** `default-src 'self'; style-src 'self' 'unsafe-inline'` — كلفة صفرية لتطبيق كل أصوله محلية.

**٣-ب (منخفضة) — ثغرتا DoS في تبعية ترانزيتية**
`cargo audit`: ‏**quick-xml 0.39.4** — ‏RUSTSEC-2026-0194 (زمن تربيعي عند فحص تكرار الخصائص) وRUSTSEC-2026-0195 (استنزاف ذاكرة عبر إعلانات نطاقات غير محدودة). تأتي عبر سلسلة Tauri ولا يمر عبرها XML من مصدر خارجي في نسق — الخطر العملي شبه معدوم. إضافةً: 17 تحذيرًا (حزم unic-*‎ غير مصانة، وunsoundness في glib 0.18.5 — ‏RUSTSEC-2024-0429). **npm audit: صفر ثغرات.** **المقترح:** `cargo update` دوري مع ترقيات Tauri.

**سليم بعد الفحص:**
- **سطح أوامر IPC (11 أمرًا):** كلها أدنى صلاحية. الملفات محصورة في مجلد بيانات التطبيق؛ `export_drafts` (`src-tauri/src/shared/drafts.rs:40`) يعقّم اسم الملف (`/` `\` `:` ويرفض `.` و`..`) فلا اجتياز مسار، والوجهة مجلد التنزيلات حصرًا والمحتوى نسخة حرفية من drafts.json لا غير.
- **القدرات:** `src-tauri/capabilities/default.json` — ‏`core:default` فقط؛ لا إضافات fs/shell/http إطلاقًا. نموذجي.
- **المفتاح:** يُقرأ من settings.json فقط، الملف يُقفل 0600 بعد الحفظ (`src-tauri/src/shared/settings.rs:83`)، لا يُسجَّل ولا يظهر في أي رسالة خطأ (رسائل llm.rs كلها ثابتة بلا إدراج)، ولا يُرسل إلا `bearer_auth` إلى `base_url` المضبوط، ولا يلمس المسودات أو التصدير أو سجل التجربة. حقل الإدخال `type=password` مع `autocomplete=off`.
- **XSS:** لا `innerHTML` بمحتوى إلا تفريغًا (`= ""`)؛ مخرجات النموذج والمسودات والملاحظات كلها `textContent` — اختُبر بالحقن فعليًا.
- **localStorage:** ‏`nasaq-theme` يُصادَق على قائمة الثيمات، و`nasaq-appearance` مقارنة صارمة، ومفتاح `nasaq-shadhb` مقارنة نصية لا تنفيذ، والتلف أو الغياب يسقط للوضع الافتراضي بأمان في الثلاثة.
- **Rust:** لا `unsafe` واحدة في المشروع. خارج الاختبارات لا يوجد إلا `expect` إقلاع Tauri المعتاد (`src-tauri/src/main.rs:34`) و`expect` على مسار ثابت وقت الترجمة (`src-tauri/src/shadhb_trial_log.rs:21`) — كلاهما غير بالغ من مدخلات المستخدم. أخطاء التفكيك كلها `Result` برسائل عربية.

---

## ٤) المعمارية / الاتساق

**٤-أ (منخفضة) — تعليقات بصيغة المضارع تشير إلى ملفات زالت**
البند المعروف مؤكَّد وأوسع قليلًا مما ذُكر:
- `src-tauri/src/shadhb/mod.rs:2` «لا واجهة بعد — الأمر مسجَّل لكن لا زر يناديه» و`src-tauri/src/shadhb/commands.rs:2` «لا واجهة تناديه بعد» — الواجهة قائمة منذ v5.0.
- إحالات `main.js` حاضرة الصيغة: `src/index.html:64` «تُملأ من ثوابت main.js — مصدر الحقيقة الوحيد»، `src/styles.css:466` «تُضبط ديناميكيًا من main.js»، `src-tauri/src/nasaq/contracts.rs:7` «يجب أن يطابق قوائم main.js».
- إحالات `main.rs` حاضرة الصيغة في nasaq.js: الأسطر 39 و61 و90 — الثوابت والعقود في contracts.rs الآن. (عبارات «نُقل من main.rs حرفيًا» التاريخية سليمة وتبقى.)
لا «v4.4» مضلِّلة — ذكراها توثيق تاريخي صحيح.

**٤-ب — عزل البرجين: لا ملاحظات.** لا `use crate::nasaq` في شذب ولا العكس (grep صفري)؛ nasaq.js لا يذكر شذب وشذب لا يلمس دوال نسق ولا يكتب في الخانة إلا عبر `sendToNasaq` — والحرّاس الآليون في `tests/isolation.test.js` يسيّجون كل ذلك.

**٤-ج — اتساق النسخة: لا ملاحظات.** ‏6.1.0 في package.json وCargo.toml وtauri.conf.json وpackage-lock.json وCargo.lock واسم dmg في README؛ والتذييل «V6.1» يوافق عرف major.minor المتبع منذ v6.0 (تحقُّق من لقطة git السابقة).

**٤-د — الاختبارات: لا ملاحظات.** ‏Node: ‏90/90 نجاح، صفر متخطى. ‏Rust: ‏48/48 نجاح. لا اختبار هشّ ظهر، وملف الحالات المشتركة يستهلكه الطرفان فعلًا (Node قراءةً وRust تضمينًا وقت الترجمة).

**٤-هـ (معلومة)** — ‏`data/shathb-trials/` في ‏.gitignore فعلًا — نصوص الكاتب الخاصة في سجل التجربة **لا** تُرفع مع المستودع. سليم.

---

## ٥) البنود المعروفة — إعادة التحقق

- **مطابقة القصّ بأول ظهور بلا حد أدنى للطول:** **مؤكَّد قائمًا** — التفاصيل والمثال الحي في قسم شذب أدناه (بند ش-١).
- **shadhb.js بلا اختبارات سلوكية:** **مؤكَّد** — التفاصيل في قسم شذب (بند ش-٥).

---

## شذب — رصد فقط، لا تنفيذ (تجميد التجربة ساري)

> كل ما يلي **ملاحظات تُسجَّل ولا تُنفَّذ** — لا تغيير في shadhb/‏ ولا shadhb.js ولا عقوده حتى تكتمل تجربة الـ20–30 شذرة، عملًا بقرار 2026-07-08.

**ش-١ (عالية كرصد) — أول ظهور دائمًا، بلا حد أدنى لطول القصّة — مؤكَّد بمثال حي**
`src/prune.js:30` (`indexOf` من البداية)، و`src/shadhb.js:218` لا يمرّر `occurrence` أبدًا (رغم أن prune.js و`src-tauri/src/shadhb/verify.rs:20` يدعمانه!)، و`src-tauri/src/shadhb/commands.rs:66` يتحقق بـ`contains` (سلسلة جزئية، لا حدود كلمات، وأدنى طول كلمة واحدة فقط).
**مثال مُشغَّل فعليًا في هذا التدقيق:** الأصل: «**ما تبقى** من الحلم **ما تبقى** منا. وكل ما تبقى يذوب في نهاية الأمر.» — اقتراح النموذج قصّ «ما تبقى» (يقصد الحشو اللاحق) → «احذف» حذف **فاتحة الشذرة** فصارت: «من الحلم ما تبقى منا. وكل ما تبقى يذوب...» **وشهادة العهد مرّت خضراء بصمت** (الناتج ما زال تتابعًا من كلمات الأصل) — قصّ في الموضع الخطأ لا يلتقطه أي حارس.
**ومثال ثانٍ (داخل كلمة):** قصّ «نا.» من «هنا.» ترك «ه» — هنا الشهادة **التقطته** وأعلنت الخرق وحجبت الإرسال، لكن بعد وقوع القصّ في المعاينة.
**المقترح (بعد التجميد):** تمرير `occurrence` من الواجهة (البنية جاهزة في الطرفين)، ومطابقة على حدود الكلمات، وحد أدنى للطول أو رفض الاقتباس الغامض متعدد الظهور.

**ش-٢ (متوسطة كرصد) — القصّ المطبَّق لا تراجعَ عنه محليًا، والنسخ لا يتحقق من الشهادة**
`src/shadhb.js:215` — «احذف» نهائي: لا زر تراجع في شذب، والعلاج الوحيد فحص جديد (نداء نموذج يُدفع ثمنه). و`src/shadhb.js:273` — «نسخ» ينسخ `state.currentText` حتى حين تعلن الشهادة الخرق وتحجب الإرسال (زر الإرسال يُحجب، والنسخ لا). **المقترح (بعد التجميد):** لقطة تراجع واحدة لكل قصّ، وربط النسخ بنجاح الشهادة أو تنبيه.

**ش-٣ (متوسطة كرصد) — ألوان شذب لا تصمد في الوضع الليلي (أُضيف في v6.1 بعد تجميدها)**
قياسات فعلية (WCAG AA للنص الصغير 4.5:1):
- تلميح العهد `#9c5238` **مقسوّ في CSS** ولا يتكيف مع الليلي (`src/styles.css:1465`): ‏3.15:1 ليلًا (فاتحًا 4.99 ✓).
- شريط الضمانة `--verify-green` غير مُعاد تعريفه ليلًا: ‏**2.94:1** (وفاتحًا 3.26 — دون AA في الحالين).
- الاقتباس المشطوب `--prune-accent`: ‏3.32 ليلًا / 3.57 فاتحًا.
- نص زر «افحص الشذرة»: ‏3.75 ليلًا / 4.27 فاتحًا.
مرئي بوضوح في لقطة الليلي. **المقترح (بعد التجميد):** نسخ ليلية للمتغيرات الأربعة داخل كتلة `[data-appearance="dark"]` — تغيير CSS خالص لا يمس السلوك.

**ش-٤ (منخفضة كرصد) — صياغة الإسقاط لا تغطي كل أسبابه**
`src/shadhb.js:147` — «لم يطابق النص حرفيًا أو تجاوز سقف الحجم» بينما الإسقاط في `src-tauri/src/shadhb/commands.rs:69` يشمل أيضًا «النص كله» و«فوق العدد الأقصى».

**ش-٥ (البند المعروف — مؤكَّد) — shadhb.js بلا اختبارات سلوكية**
كل ما يغطيه `tests/isolation.test.js` فحصُ **نصوص المصدر** (grep): وجود الدرع والبوابة وغياب النداءات المحظورة. منطق `applyCutAt` (بما فيه حالة stale) و`renderCovenant` و`presentResult` وحالتا الفراغ — كلها بلا اختبار تشغيل. (وحدتا prune.js وverify.rs النقيتان مغطاتان جيدًا بالحالات المشتركة.)

**ش-٦ (منخفضة كرصد) — مسار سجل التجربة مربوط بجهاز البناء**
`src-tauri/src/shadhb_trial_log.rs:19` — المسار من `CARGO_MANIFEST_DIR` (ثابت وقت الترجمة). نسخة .app منقولة لجهاز آخر أو بعد نقل المستودع ستفشل بالكتابة (بخطأ ظاهر، لا صامت). مقبول لأداة مؤقتة على جهاز واحد؛ للعلم.

**سليم في شذب بعد الفحص:** البوابة الآلية في `src-tauri/src/shadhb/verify.rs:247` **قائمة ولا التفاف عليها** — كل قصّة من النموذج تمر إجباريًا بحرفية `contains` + «ليست النص كله» + سقف (≤14 كلمة **و**≤نصف النص) في `src-tauri/src/shadhb/commands.rs:66`، وبنية `PruneResult` لا تملك حقل نص كامل أصلًا فيستحيل تسريب «نسخة محسّنة» (اختبار يثبته)؛ العدّ يُحسب محليًا لا من ادعاء النموذج؛ حرارته وميزانيته (0.3 / 1024) في برجه وحده؛ والشهادة تُفحص حيّة بعد كل قرار وتحجب الجسر عند الخرق (اختُبر حيًّا)؛ وحارس تغيّر الخانة بعد الفحص يعمل.

---

## الخلاصة

الأداة في حالة صحية جيدة جدًا لعهدها: العزل صارم ومسيَّج آليًا، قفل التكلفة صامد في كل مسار، سطح IPC نظيف، ولا ثغرة XSS أو اجتياز مسار. أهم ثلاثة بنود قابلة للتنفيذ الآن (كلها خارج شذب): **حارس ⌘+Enter في وضع شذب** (١-أ)، **علم انشغال مشترك للنداءات** (٢-أ)، و**تفعيل CSP** (٣-أ). وأثقل ما يُسجَّل لما بعد التجميد: قصّ أول ظهور الذي قد يحذف الموضع الخطأ وتمرّ شهادته صامتة (ش-١)، وألوان شذب الليلية (ش-٣).
