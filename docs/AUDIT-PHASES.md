# مراحل فحص NSQ

مسارٌ مستقل عن مسار البناء (`docs/PHASES.md`): ذاك بنى الواجهة من التصميم، وهذا يفحص التطبيق الذي نتج. مصدره قائمة فحص وضعها المالك — ٢٣ بندًا في أربعة محاور، وقواعد عمل — محفوظة كما هي في `.local/audit/SOURCE.md`، وحُوّلت هنا إلى ثماني مراحل تناسب المشروع. **لم يُحذف بندٌ لأنه لم يُعجب:** كُيّف ما يخالف طبيعة التطبيق، وحُذف ما لا يفيد وحده، ومصير كل بند بسببه في الملحق آخر الملف.

قاعدة العزل في `CLAUDE.md` ملزمة هنا كما في البناء، و`tests/isolation.test.js` يبقى أخضر بعد أي تعديل.

- **الفرع:** `audit/v27`، في شجرة عمل مستقلة `/Volumes/iSltanXx/Projects/NSQ-audit` — فلا يتبدّل فرعٌ تحت جلسة البناء في المجلد الرئيسي. يأخذ آخر `feature/v27-ui` بالدمج قبل كل مرحلة، ولا تعود إصلاحاته إليه إلا بقرار المالك.
- **الحالة اللحظية** في `AUDIT-STATUS.md` بجذر شجرة الفحص.
- **تفاصيل كل مرحلة** في `.local/audit/<mN>-<اسم>/` بجذر شجرة الفحص — `PLAN.md` قبلها و`REPORT.md` بعدها — محلية غير متتبَّعة.
- **سجلّ الملاحظات** واحد لكل المراحل: `.local/audit/LEDGER.md`.
- **مرجع التصميم**: ملف Figma `nasq-v10`، المفتاح `DZQACOUgD70llKedxWcHbK` — الحَكَم في كل خلاف بين الكود والشكل.
- **أداة القياس في المتصفح**: `.playwright-mcp/nsq-runners/` من المرحلة ٨ — `audit.js` للتباين والفيضان والحركة، و`runner-*` للحالات والأوضاع ولوحة المفاتيح، و`tauri-mock.js`. محلية في المجلد الرئيسي للمستودع لا في شجرة الفحص، ومرآة المعاينة تُبنى من `src/` في شجرة الفحص.

## الخريطة

| # | المرحلة | بنود القائمة | الحالة | يعتمد على | التطبيق الحقيقي | الإيداع |
|---|---|---|---|---|---|---|
| m1 | خط الأساس | — | **التالية** | — | لا | — |
| m2 | سلامة البيانات والحفظ | ١٨، ٢٢ | لم تبدأ | m1 | لا | — |
| m3 | الصلابة والأداء | ١٧، ١٩، ٢١ | لم تبدأ | m1 | لا | — |
| m4 | السلوك التفاعلي ولوحة المفاتيح | ١–٥، ١١، ١٣، ١٤، ١٦ | لم تبدأ | m2، m3 | لا | — |
| m5 | نظام التصميم في الكود | ٧، ٨، ٩، ٢٠ | لم تبدأ | m4 | لا | — |
| m6 | التمريرة البصرية | ٥، ٦، ٧–٩، ١٠، ١٢، ١٥ | لم تبدأ | m5 | لا | — |
| m7 | جلسة التطبيق الحقيقي | ٦، ١٠، ١١، ١٣، ١٦، ١٧، ٢٢ | لم تبدأ | m6 | **نعم — بإذن، مرة واحدة** | — |
| m8 | التنظيف والإغلاق | ٢٣ | لم تبدأ | m7 | تأكيدٌ قصير بإذن إن لزم | — |

البند الذي يظهر في مرحلتين ينقسم بينهما: سلوكًا ثم شكلًا، أو في المتصفح ثم في التطبيق.

## لماذا هذا التقسيم

- **الأخطر أولًا.** ضياع نصّ الكاتب أسوأ من أي عيب في الشكل، فسلامة البيانات (m2) والصلابة (m3) قبل كل شيء.
- **الكود قبل الشكل.** m2–m5 تغيّر JS وRust وCSS، والتمريرة البصرية (m6) تقيس الناتج النهائي مرة واحدة — على قرار «الفحص البصري تمريرة واحدة».
- **التطبيق الحقيقي مرة واحدة.** كل ما لا يظهر إلا أصليًا — الإغلاق وإعادة الفتح، واختصارات النظام، والسحب من تطبيق آخر، وأرقام الذاكرة — مجموعٌ في m7، على قاعدة «لا تشغيل بلا إذن».
- **الأداة تتدرّج ولا ترجع:** قراءة الكود ← الاختبارات ← المتصفح ← التطبيق.
- **التنظيف آخرًا.** إصلاحات m2–m7 قد تُميت كودًا أو تحييه؛ حذف الميت قبلها يعيد العمل.
- **ما بعد المرحلة ٨ لم يُفحص.** كل ما أُودع بعد `cc4b5d9` (خاتمة تمريرة الجودة) لم يمرّ بها؛ m1 يحصره، وكل مرحلة تبدأ به.

## المراحل

### m1 — خط الأساس
**الهدف:** أرقام حقيقية يُقاس عليها كل ما بعدها، وأداة قياس جاهزة، وسجلّ ملاحظات واحد.
**الطريقة:** `npm test` و`cargo test` و`cargo clippy` بأرقامها الفعلية، وحصر ما أُودع بعد `cc4b5d9` وأيّ مرحلة تفحص كلَّ إيداع، وتشغيل `audit.js` على الكود الحالي مع التأكد أنه يقرأ نسخة طازجة، وإنشاء `LEDGER.md`.
**معيار الاكتمال:** الأرقام من مخرجات أوامر هذه الجلسة لا من `STATUS.md`، والأداة تُخرج تقريرها، والسجلّ منشأ بأعمدته. لا إصلاح في m1.

### m2 — سلامة البيانات والحفظ
**الهدف:** لا يضيع نصٌّ ولا مسودة ولا إعداد، ولا تعرض لوحةٌ حالةً قديمة.
**البنود:** ٢٢ الحفظ التلقائي والتخزين المحلي · ١٨ تزامن الحالة.
**الطريقة:** جدولٌ بكل مسار كتابة وقراءة (ماذا، متى، أين، بأي أمر، وما يحدث عند الفشل)، واختبارات Rust لسلامة الكتابة، واختبارات `drafts-model.js` للاستيراد والدمج، وسيناريوهات تزامن في المتصفح بين المحرر والشريط الجانبي والمفتّش وشريط الحالة والقوائم ونافذة الإعدادات.
**معيار الاكتمال:** كل مسار بحكم، وكل عيب مؤكَّد مُصلح بحارس، وما يحتاج التطبيق الحقيقي منقول إلى m7.
**يُطرح على المالك:** حفظ النص الجاري واسترجاع الجلسة، وتنبيهٌ قبل الإغلاق بصيغ غير محفوظة — قرار منتج لا إصلاح.

### m3 — الصلابة والأداء
**الهدف:** لا انهيار ولا تجمّد مع أي مدخل أو أي عطل في الشبكة، ولا تسرّب.
**البنود:** ١٩ الأخطاء الاستثنائية · ٢١ عزل المعالجة عن الواجهة · ١٧ الذاكرة والمعالج (قراءةً وقياسًا في المتصفح).
**الطريقة:** مدوّنة إجهاد في `tests/fixtures/` (فارغ، مسافات، نص ضخم، رموز تعبيرية مركّبة، تشكيل وتطويل، أسطر طويلة، CRLF) تمرّ على الدوال الصرفة؛ وردود مزوّد تالفة ومهلات وأخطاء HTTP في اختبارات Rust؛ وقياس المهام الطويلة والذاكرة في المتصفح بعد تكرار التنقّل.
**معيار الاكتمال:** كل مسار خطأ برسالة عربية واضحة ويُبقي نصّ الكاتب، ولا مهمة أطول من ٥٠ م.ث أثناء الكتابة العادية، ولا نمو في الذاكرة مع التكرار، وكل أمر متزامن يلمس القرص أو سلسلة المفاتيح مقيسٌ بحكم.

### m4 — السلوك التفاعلي ولوحة المفاتيح
**الهدف:** كل عنصر يُجيب كما يُجيب عنصرٌ في تطبيق ماك أصلي، وكل أمر يصل من لوحة المفاتيح.
**البنود:** ١ الاستجابة والتغذية الراجعة · ٢ حالات التحميل · ٣ الشاشات الفارغة · ٤ الحذف والتراجع · ٥ الاتجاه العربي سلوكًا · ١١ حركة طيّ الشريط الجانبي · ١٣ الاختصارات · ١٤ التنقل بالأسهم · ١٦ السحب والإفلات (الموجود منه، والقرار).
**الطريقة:** ثلاثة جداول: كل عنصر تفاعلي بحالاته (مرور، ضغط، تركيز، تعطيل، مؤشر)؛ وكل أمر × الوحدة × الحالة عبر حدث `menu:action` الحقيقي؛ وكل فعل هادم بتأكيده أو تراجعه.
**معيار الاكتمال:** الجداول الثلاثة بلا خانة فارغة، وعيوب الكود مصلحة بحرّاس.
**يُطرح على المالك:** تراجعٌ عن «احذف القصّة» في شَذْب، ودعم إسقاط ملف نصّي في المحرر — تصميمٌ في Figma أولًا إن قُبلا.

### m5 — نظام التصميم في الكود
**الهدف:** كل لون ومسافة وخط يأتي من التوكنز المولَّدة من Figma، ولون التأكيد في موضعه وحده.
**البنود:** ٢٠ توحيد المتغيّرات · ٨ المسافات · ٩ سلّم الخطوط · ٧ دلالة لون التأكيد.
**الطريقة:** مقابلة `src/tokens.css` بمتغيّرات Figma، وتصنيف كل قيمة خام في CSS (خط شعري مقصود، أو مقياس ماك مقيس، أو توكن ناقص)، وجرد استعمالات `--accent-*` و`--module-*` بأدوارها، وجرد أدوار النص بتوكنزها.
**معيار الاكتمال:** كل قيمة خام مصنّفة، وحارسٌ يمنع اللون الخام خارج `tokens.css` مع قائمة بيضاء للقيم الخام المقصودة — كلاهما كُسِر عمدًا وفشل قبل اعتماده. ولا قيمة تتغيّر إلا من Figma عبر `tools/figma/generate-tokens.js`.

### m6 — التمريرة البصرية
**الهدف:** الواجهة كما رُسمت في كل عرض ومظهر، مقروءة ومتاحة.
**البنود:** ١٠ مرونة الحجم · ١٢ تباعد الأسطر والقطع · ٦ التباين والنفاذية · ١٥ الوضع الداكن · ٥ الاتجاه العربي بصريًا · ٧–٩ حكمًا بالعين مقابل الإطارات.
**الطريقة:** `audit.js` في أوضاع المرحلة ٨ كلها على مسح عروض من ٧٦٠ صعودًا — لا عرضين فقط —، وLighthouse للنوافذ الثلاث، ومقابلة كل سطح تغيّر بعد المرحلة ٨ بإطاره في Figma، وجدول قطع للنصوص الطويلة.
**معيار الاكتمال:** صفر خرق تباين وصفر فيضان في كل عرض ممسوح، و100/100 وصولًا، وكل سطح متغيّر مقابَل بإطاره، وفخّا المرحلة ٨ مفحوصان: الطبقات الشفافة، والنص بارتفاع صفر.

### m7 — جلسة التطبيق الحقيقي
**الهدف:** ما لا يُرى إلا في التطبيق نفسه، في تشغيلٍ واحد بإذن المالك.
**البنود:** ١١ حفظ حالة الألواح عبر الإغلاق · ١٣ اختصارات النظام · ١٦ السحب والإفلات من تطبيقات أخرى · ١٧ الذاكرة والمعالج بأرقام أصلية · ٢٢ الإنهاء المفاجئ · ١٠ سحب حجم النافذة وملء الشاشة · ٦ VoiceOver إن أراد المالك.
**الطريقة:** بناء إصدار بمعرّف مستقل للفحص فلا تُمسّ مسودات المالك ولا إعداداته، ونصٌّ نموذجي، وقائمة خطوات مكتوبة سلفًا تُنفَّذ في تشغيل واحد. نداءات النموذج بمفتاح المالك تُذكر بعددها قبل الإذن.
**معيار الاكتمال:** كل خطوة بنتيجة ودليل من هذه الجلسة، وبيانات المالك لم تُمسّ (بصمة الملف قبل وبعد).

### m8 — التنظيف والإغلاق
**الهدف:** لا كود ميتًا ولا اعتمادًا زائدًا، والسجلّ مغلق.
**البنود:** ٢٣ الكود الميت والاعتمادات.
**الطريقة:** محدِّدات CSS بلا عنصر، ودوال بلا نداء، وملفات بلا مرجع، واعتمادات Rust بلا استعمال — كلٌّ يُثبَت بالبحث مع الأسماء المبنيّة في JS —، ثم انحدار كامل: الاختبارات وclippy والبناء و`audit.js` وحجم الحزمة مقابل ٩٫٩ م.ب.
**معيار الاكتمال:** كل حذف بسببه، والانحدار أخضر، وكل صفٍّ في السجلّ بقرار. ثم تعود المرحلة ٩ إلى ما بقي منها.

## قواعد الفحص

1. **مرحلة واحدة ثم توقّف.** تنتهي المرحلة بتقريرها، ولا تبدأ التالية إلا بـ«ابدأ mN». *لماذا:* ما يُبنى على نتائج لم يرها المالك قد يُهدم.
2. **الدليل قبل الحكم.** كل ملاحظة تحمل `ملف:سطر` أو مخرج أمر، وإحدى ثلاث صفات: **مؤكَّد** (شُغّل أو اختُبر)، أو **من القراءة** (قُرئ في الكود ولم يُشغَّل)، أو **[غير محدد - بانتظار التفاصيل]** حين لا يجيب الكود ولا الأدوات. لا يُسدّ نقصٌ بتخمين. *لماذا:* الحكم الواثق بلا دليل أخطر من «لا أعرف».
3. **القياس قبل الإصلاح، والإصلاح في نطاقه.** العيب البرمجي معلوم السبب يُصلح في مرحلته بحارسٍ كُسِر عمدًا وفشل قبل اعتماده. ما يغيّر الشكل أو يضيف سلوكًا يُصمَّم في Figma أولًا بقرار المالك. وما يمسّ برومبتًا أو عقدًا في نَسَق أو شَذْب يوقف العمل ويُسأل عنه. *لماذا:* التصميم يقود، والعزل فوق كل شيء، والفحص لا يصير بناءً خفيًا.
4. **مراجعة ذاتية قبل التقرير:** تُعاد الاختبارات، ويُفتح موضع كل ملاحظة من جديد ويُطابَق اقتباسه، ويُبحث عن الكود الذي ينقضها — فما صمد بقي. *لماذا:* في المرحلة ٨ كان مدقّق التباين نفسه هو المخطئ، وكان نصٌّ بارتفاع صفر لا يراه أي مدقّق.
5. **تسليمٌ صافٍ بين المراحل.** كل تقرير ينتهي بـ«المدخل للمرحلة التالية» بالنتائج الصافية وحدها، والمرحلة التالية تبدأ منه ومن `AUDIT-STATUS.md` لا من المحادثة — ويُفضَّل في محادثة جديدة. *لماذا:* Claude لا ينسى بأمر؛ السياق النظيف ملفٌّ مكتوب وجلسة جديدة.
6. **المخرجات جداول ونقاط** بأعمدة ثابتة (صيغة التقرير أدناه)، بلا سرد.
7. **المؤلف واحد.** الحكم في الهوية والتصميم للجلسة الرئيسية وحدها. الوكلاء — على Sonnet 5 فقط — يبحثون ويكتبون الكود ويراجعون: `test-engineer` للاختبارات، و`code-reviewer` بعد كل إصلاح، و`debugger` للأعطال، و`ui-ux-guardian` قراءةً فقط، وحارسا البرجين إن اقترب تعديلٌ من عقد.
8. **التطبيق الحقيقي بإذن.** لا تشغيل ولا تسجيل شاشة إلا في m7 وبإذنٍ في حينه.
9. **الإيداع والدفع.** إيداعٌ على `audit/v27` لكل إصلاح متحقَّق بمساراته صراحةً، ثم دفعه. لا `main` ولا وسم، ولا دمج في `feature/v27-ui` إلا بقرار المالك. *لماذا:* مسار البناء يعمل في الوقت نفسه، وفرعٌ لكلٍّ منهما يُبقي إصلاحات الفحص ظاهرةً قبل أن تنتقل.

## بروتوكول «ابدأ mN»

بالترتيب، قبل أي فحص:

1. `AUDIT-STATUS.md` — أين وصل الفحص والمعلّقات.
2. المرحلة في هذا الملف، وقواعد الفحص.
3. `.local/audit/<mN>-<اسم>/PLAN.md`، و`LEDGER.md`، وتقرير المرحلة السابقة وحده.
4. في شجرة الفحص: `git status`، ثم دمج آخر `feature/v27-ui` في `audit/v27` ليرى الفحص ما سيُشحن — وإن تعارض الدمج يُوقَف ويُسأل —، ثم `git log` منذ إيداع المرحلة السابقة — وما تغيّر فيه يدخل الفحص. والأسطر المذكورة في `PLAN.md` تُعاد قراءتها قبل الاعتماد عليها، فالكود يتحرّك.
5. عرض خطة التنفيذ: الخطوات، والأدوات، وهل تحتاج تشغيل التطبيق أو نداءات نموذج أو قرارًا من المالك. ما يحتاج إذنًا ينتظره، وما سواه يبدأ.
6. التنفيذ، ثم المراجعة الذاتية، ثم التقرير.

## في نهاية كل مرحلة

الاختبارات كاملة (`npm test` مع بقاء `isolation.test.js` أخضر، و`cargo test`)، ثم `REPORT.md` المحلي بصيغته، ثم `LEDGER.md`، ثم `AUDIT-STATUS.md`، ثم خريطة هذا الملف (الحالة والإيداع)، ثم إيداعٌ على `audit/v27` ودفعه — ثم التوقّف.

## صيغة التقرير

```markdown
# mN — <الاسم> · تقرير
**الحالة:** مكتملة | ناقصة (السبب) · **الإيداع:** <hash> | لا تعديل

## النتائج
| رقم | البند | الموضع | الدليل | الدرجة | الصفة | القرار |

## ما أُصلح
| رقم | الإصلاح | الحارس (كُسِر عمدًا وفشل؟) | الإيداع |

## أسئلة للمالك

## المراجعة الذاتية
- الاختبارات: npm …/… · cargo …/… · isolation أخضر
- كل ملاحظة أُعيد فتح موضعها وجُرّب نقضها

## المدخل للمرحلة التالية
- النتائج الصافية وحدها
```

- **الرقم:** `mN-01`، `mN-02`… ويبقى ثابتًا في `LEDGER.md`.
- **الدرجة:** حرِج (ضياع بيانات أو انهيار) · عالٍ (عطلٌ يراه المستخدم) · متوسط · تجميلي.
- **الصفة:** مؤكَّد · من القراءة · [غير محدد - بانتظار التفاصيل].
- **القرار:** أُصلح · Figma أولًا · قرار المالك · مقبول (بسببه).

## ملحق: مصير بنود القائمة

| # | البند | المصير | المرحلة | ما تغيّر ولماذا |
|---|---|---|---|---|
| ١ | استجابة الأزرار (Hover & Interaction) | مُكيَّف | m4 | «تغيّر المؤشر على كل زر» يخالف الماك: الأزرار بالسهم، واليد للروابط وحدها، ومؤشر الكتابة للنص. بقي الفحص كله: مرور وضغط وتركيز وتعطيل لكل عنصر، والمؤشر على عرف الماك. |
| ٢ | حالات التحميل (Loading & Spinners) | مُكيَّف | m4 | للتطبيق حالة معالجة مرسومة (تعطيل، `aria-busy`، مؤشر، هيكل تقرير) لا «Spinner» عام. بقي: ظهورها لحظيًا ومنع التكرار، ومنه ⌘↩ من القائمة أثناء العمل. |
| ٣ | الشاشات الفارغة | كما هو | m4 | المسودات، والنتيجة، وشَذْب قبل الفحص، و«لا قصّات»، وبحثٌ بلا نتيجة. |
| ٤ | الحذف والتراجع | مُكيَّف | m4 | لا «كروت قضايا» في التطبيق. يقابلها: حذف مسودة أو صيغة، و«جلسة جديدة»، و«احذف القصّة» في شَذْب، و«استيراد ودمج». |
| ٥ | سلامة الاتجاه العربي | كما هو | m4، m6 | سلوكًا (الأسهم، مؤشر الكتابة) ثم بصريًا. |
| ٦ | التباين والنفاذية | كما هو | m6، m7 | مبنيٌّ على المرحلة ٨: يُعاد على ما تغيّر بعدها، وVoiceOver — المعلّق منذ ٨ — في m7 إن أراد المالك. |
| ٧ | دلالة لون التأكيد | كما هو | m5، m6 | لونان للبرجين، ولون الهوية ثابتٌ لما هو على مستوى التطبيق. |
| ٨ | المسافات | كما هو | m5، m6 | — |
| ٩ | سلّم الخطوط | كما هو | m5، m6 | خطّا الدار Almarai وCairo ثابتان، وليسا محلّ مراجعة. |
| ١٠ | مرونة الحجم | كما هو | m6، m7 | — |
| ١١ | حالة الشريط الجانبي | كما هو | m4، m7 | الحركة في المتصفح، والحفظ عبر الإغلاق في التطبيق. |
| ١٢ | تباعد الأسطر والقطع | كما هو | m6 | — |
| ١٣ | اختصارات لوحة المفاتيح | كما هو | m4، m7 | — |
| ١٤ | التنقل بالأسهم | كما هو بمقابله | m4 | «المسودات وكروت القضايا» = شجرة المسودات وقائمة القصّات. |
| ١٥ | درجات الوضع الداكن | كما هو | m6 | — |
| ١٦ | السحب والإفلات | مُكيَّف | m4، m7 | إسقاط الملفات غير مبنيّ ولا مرسوم: إضافته قرار منتج وتصميم لا إصلاح. بقي: فحص الموجود (السحب إلى الخارج، وإفلات النص) وعرض القرار. |
| ١٧ | الذاكرة والمعالج | كما هو | m3، m7 | — |
| ١٨ | تزامن الحالة | كما هو بمقابله | m2 | لا قسم باسم «اللقطات»؛ يقابلها صيغ المسودة ولقطات التراجع في النتيجة. و«التحديث اليدوي» لا وجود له في تطبيق سطح مكتب؛ معناه هنا: لا لوحة بحالة قديمة. |
| ١٩ | الأخطاء الاستثنائية | كما هو | m3 | أُلحقت به أعطال الشبكة والمزوّد، لأنها استثناءات من الجنس نفسه وقيمة التطبيق كلها تمرّ بها. |
| ٢٠ | توحيد المتغيّرات | كما هو | m5 | التوكنز مولَّدة من Figma؛ الإصلاح يمرّ به لا بتحرير يدوي. |
| ٢١ | عزل المعالجة عن الواجهة | مُكيَّف | m3 | نداءات النموذج في Rust غير متزامنة أصلًا؛ فينصبّ الفحص على الأوامر المتزامنة (القرص، سلسلة المفاتيح) والأدوات المحلية في JS. |
| ٢٢ | الحفظ التلقائي | مُكيَّف | m2، m7 | نموذج التطبيق حفظٌ صريح (⌘S) وإعدادات فورية. الفحص: ما يضيع عند الإغلاق أو الانهيار، وسلامة الكتابة. والحفظ التلقائي نفسه قرار منتج. |
| ٢٣ | الكود الميت والاعتمادات | كما هو | m8 | آخرًا عمدًا. |

**الحصيلة:** ١٥ كما هي، و٢ بمقابلهما في التطبيق، و٦ مُكيَّفة، وصفر محذوف.

**قواعد العمل في البرومبت:**

| القاعدة | المصير | لماذا |
|---|---|---|
| الصفة: قائد تقني ومصمم تنفيذي «بلمسة رجل واحد» | كما هي، مختصرة | تطابق قاعدة «المؤلف واحد» (القاعدة ٧). |
| الالتزام ببيانات الكود، و`[غير محدد - بانتظار التفاصيل]` | كما هي، وصفتان معها | أُضيفت «مؤكَّد» و«من القراءة» ليُعرف مصدر كل حكم (القاعدة ٢). |
| التنفيذ المرحلي | كما هو | بنقطة توقّف صريحة (القاعدة ١). |
| المراجعة الذاتية | كما هي، بخطوات | «راجع نفسك» وحدها لا تُنفَّذ؛ صارت ثلاث خطوات محددة (القاعدة ٤). |
| المخرجات المنظمة | كما هي | بصيغة تقرير ثابتة. |
| تطهير الذاكرة والسياق | مُكيَّفة | Claude لا ينسى بأمر؛ البديل الفعلي ملفّ تسليم وجلسة جديدة (القاعدة ٥). |
| «لتطوير وفحص الكود» | مُكيَّفة | فحصٌ وإصلاح؛ والميزة الجديدة قرار المالك وتصميمٌ أولًا (القاعدة ٣). |
| «اقرأ المشروع وقسّمه بنفسك إلى مراحل» | **محذوفة** | القسمة ثابتة في هذا الملف؛ إعادتها كل جلسة تُنتج قسمةً مختلفة كل مرة وتستهلك السياق. |
| «اعرض التقسيم وانتظر إشارتي» | كما هي | صارت بروتوكول «ابدأ mN». |
| الصياغة الشديدة («صارمة»، «مطلق»، «فورًا») | مُليَّنة | Claude ينفّذ التعليمات حرفيًا، والتشديد يدفعه إلى الإفراط في تطبيقها؛ القاعدة المعلَّلة أدقّ أثرًا. |
