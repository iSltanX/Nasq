# سجل إصدارات نَسَق

> نُقل هذا السجل من `README.md` عند تحويله إلى واجهة تعريف احترافية للمستودع. المحتوى كما هو دون تغيير.

## سجل الإصدارات

نظام الترقيم: **Major** (v5 → v6) لأي تغيّر واضح في تجربة الاستخدام أو وضعٍ/غرفةٍ جديدة؛ **Minor** (v5.1) لتحسينات داخل تجربة قائمة؛ **Patch** (v5.1.1) للإصلاحات الصغيرة. وقفز الرقم من 9 إلى **27** ليوافق macOS 27 الذي صُمّمت له واجهة الإطلاق.

### v27.0.0

Major — إصدار الإطلاق: إعادة بناء الواجهة كاملةً من ملف التصميم على هيئة macOS 27 الأصلية، ثم فحص التطبيق كله في ثماني مراحل (m1–m8). محرّكا نَسَق وشَذْب وعقودهما وبرومبتاتهما لم تُدمج، وعزل البرجين أخضر.

- **هيئة macOS 27 الأصلية**: إطار النافذة وإشاراتها على اليمين في تطبيقٍ عربي، وشريط قوائم أصلي بعناوين عربية تتبدّل قائمته الرابعة مع البرج، وقوائم سياق، واختصار ثابت لكل أمر. والنافذة تتكيّف حتى عرض ٧٦٠ بأدوات «المزيد ⋯».
- **نَسَق**: الواجهة كاملةً من التصميم — الأصل والنتيجة، ولوحة جانبية، ومفتّش بالتقرير وبصمة الإيقاع، وعدسة القراءة، ونتائج المنصّات بطاقاتٍ بعدّادها، وورقة التنويعات. وأداة محلية جديدة **«إضافة سطور فارغة»** (⌥⌘↵) بجوار «حذف السطور الفارغة» و«كسر بعد النقطة»، و⇧⌘Z يعيد ما تراجع عنه ⌘Z في أدوات النتيجة.
- **شَذْب**: لوح المراجعة بحالاته المرسومة — جاهز، وجارٍ الفحص، ولا قصّات، واقتراحات قديمة بعد تعديل النص، وقصّة «تعذّرت» بسببها، و«مكتمل جزئيًا» — والتراجع عن قرار القصّة بـ⌘Z و⇧⌘Z.
- **المزوّدون والإعدادات**: OpenAI وClaude إلى جانب Gemini وGroq وOpenRouter وOllama المحلي. المفتاح في سلسلة مفاتيح macOS (يُرحَّل إليها من الملف القديم تلقائيًا)، بزرّ عينٍ يُظهره وتأكيدٍ ظاهر للحفظ. و«اختبر الاتصال» يجرّب توليدًا فعليًا قصيرًا ويعرض نصّ خطأ المزوّد نفسه، لا قائمة النماذج وحدها.
- **أول تشغيل والتحديثات**: ورقة ترحيب من خطوتين لاختيار المزوّد وربطه، وتنبيهات تحديثٍ من التصميم، وتحقّقٌ تلقائي يمكن إيقافه، و«إلغاء» التحديث لا يعيد تشغيل التطبيق.
- **سلامة البيانات**: حفظٌ ذرّي لا يترك ملفًا ناقصًا، وملفٌّ تالف يُنحّى جانبًا ولا يُمحى، والجلسة الجارية (النص والنتيجة واختيارات الشريط) تعود بعد الإغلاق، وقفل النسخة الواحدة يمنع نسختين من محو مسودات بعضهما، وحدّ المسودات يرفض الزيادة بدل حذف القديم بصمت.
- **التفاعل وإمكانية الوصول**: تمريرة لوحة المفاتيح وقارئ الشاشة (Lighthouse للوصول ١٠٠/١٠٠ في النوافذ الثلاث)، وحلقة تركيز ظاهرة، وكل كتابةٍ في الخانة يُتراجع عنها، وإفلات ملفٍّ نصّي في المحرّر.
- **المزوّدون في النقل**: Gemini 3 يأخذ `reasoning_effort`، ومحاولة ثانية واحدة عند 503/529، ومهلة التوليد من سقف المخرجات.
- **الأيقونة «النون المشذَّبة»** بصيغة Icon Composer (`Assets.car`) بوجهين فاتح وداكن، وحُذف تبديل أيقونة الدوك وقت التشغيل وخطّاف `NSQ_ENTRY` ومخلّفات الهوية السابقة.
- **التوزيع**: المستودع `iSltanX/Nasq` عامّ، ونقطة التحديث ومفتاح التوقيع العام كما في v9.0.0.
- الاختبارات: ٣٩٩/٣٩٩ في Node (والعزل أخضر)، و١٧٦/١٧٦ في Rust، وبناء إصدارٍ ناجح.

### v9.0.0

Major: إعادة بناء Product UI الكاملة اعتمادًا على تصميم Figma المعتمد (استبدال بنيوي لا Reskin) — بلا أي مساس بمحرّكات نَسَق/شَذْب أو العقود أو منطق المعالجة أو المسودات.

- **واجهة جديدة من Figma**: القشرة والشريط العلوي واللوحتان وشريط أدوات نَسَق (٨ أوامر بترتيب RTL) ومساحة عمل شَذْب والإعدادات والمسودات والحالات العابرة — إعادة بناء كاملة للتخطيط والتسلسل الهرمي، لا طبقة فوق القديم.
- **بحث داخل النتيجة**: ميزة جديدة عبر CSS Custom Highlight API، بلا أي تعديل على منطق العرض القائم.
- **تحديث الهوية البصرية**: لوحة ألوان جديدة (طوبي/عنّابي) وإعادة تصدير كل أيقونات المنصّات (macOS · Windows · iOS · Android).
- **مزوّد Ollama المحلي**: خيار جديد في الإعدادات لتشغيل نَسَق/شَذْب عبر نموذج محلي بلا مفتاح API، إلى جانب المسار السحابي القائم دون تغيير في سلوكه.
- 93/93 اختبار JS، 51/51 اختبار Rust، وبناء إنتاجي موقَّع دون فروقات.

### v8.2.2

توحيد المستودعات: مستودع الكود `iSltanX/Nasq` صار عامًا، وهو المصدر الوحيد للكود والوسوم والإصدارات من الآن. Patch: تغيير نقطة التحديث فقط، بلا مساس بمحرّكات نَسَق/شَذْب أو الواجهة.

- **نقطة تحديثٍ جديدة**: مُحدِّث Tauri صار يقرأ `latest.json` من `iSltanX/Nasq` بدل قناة `Nasq-Releases` المنفصلة. هذا إصدار «جسر»: يُنشَر أيضًا على `Nasq-Releases` القديم ليصل لأي عميل لا يزال على v8.2.1، فينتقل تلقائيًّا ويعتمد نقطة التحديث الجديدة في كل فحصٍ لاحق.
- **مستودع الإصدارات القديم أُغلق**: بعد نقل إصداراته (v8.2.0، v8.2.1) وأصولها كما هي إلى `Nasq`.
- توحيد الإصدار **8.2.2** في جميع الملفات، وتحديث README وروابطه لتناسب مستودعًا عامًا.

### v8.2.1

إصدار التحقّق من التحديث التلقائي — يُسلَّم عبر المُحدِّث داخل التطبيق من v8.2.0. Patch: تغييرٌ مرئيّ صغير للتأكّد من اكتمال دورة التحديث بعد إعادة التشغيل، بلا أي مساس بمحرّكات نَسَق/شَذْب أو العقود أو المعالجة.

- **علامة تحقّق مرئية**: نصّ الحالة الابتدائيّ في قسم «التحديثات» بصفحة «حول» صار يعرض «التحديث التلقائي مُفعّل.» بدل الفراغ — دلالةٌ ظاهرة تؤكّد أن بناء 8.2.1 يعمل بعد التحديث.
- لا تغيير آخر في الواجهة أو المنطق. توحيد الإصدار **8.2.1**، وبناءٌ إنتاجيّ موقّع مع أصول المحدِّث و`latest.json` جديد يشير إلى 8.2.1.

### v8.2.0

إصدار «التمهيد» لنظام التحديث التلقائي: أول إصدارٍ يحمل مُحدِّث Tauri الرسمي، ويُثبَّت يدويًّا مرّةً واحدة لأن v8.1.0 لا يحمل محدِّثًا فلا يحدّث نفسه. لا تغيير في محرّكات نَسَق أو شَذْب أو العقود أو قواعد المعالجة.

- **محدِّث Tauri 2 الرسمي**: تسجيل `tauri-plugin-updater` (مع `tauri-plugin-process` لإعادة التشغيل الآمنة)، ونقطة تحديثٍ عامة عبر قناة الإصدارات المنفصلة `iSltanX/Nasq-Releases` (يبقى الكود المصدري خاصًّا). التوقيع بمفتاح minisign خاصّ بنَسَق يُحفَظ خارج المستودع؛ وpubkey فقط في `tauri.conf.json`، وتفعيل `createUpdaterArtifacts`.
- **آلة حالات التحديث في «حول»**: فحص → إتاحة الإصدار → تنزيل بتقدّمٍ حقيقيّ → تثبيت → إعادة تشغيل، عبر أوامر `plugin:updater` الرسمية حصرًا (لا مُنزِّل مخصّص، ولا تنفيذ يدويّ، ولا إضعاف للتحقّق — الإضافة تتحقّق من التوقيع داخليًّا). حالات idle/checking/no-update/available/downloading/downloaded/installing/restart/error برسائل عربية موجزة، ومنعٌ للازدواج، وأخطاءٌ لا تُسرّب أسرارًا أو مسارات.
- **واجهة مضغوطة داخل «حول»**: أزرار «البحث عن تحديثات / تنزيل التحديث / تثبيت وإعادة التشغيل» وشريط تقدّم ونص حالة — بخطوط الهوية (Almarai للأزرار، Cairo للحالة) وتوكنز البرج الفعّال، دون بطاقةٍ ضخمة أو خلطٍ للهوية.
- **الصلاحيات**: `updater:allow-check/download/install` و`process:default` في القدرات، وتشديد `.gitignore` على أسرار التوقيع.
- توحيد الإصدار **8.2.0**، وتحديث README (تحديث تلقائيّ فعليّ + قناة إصدارات عامة). الاختبارات: 93 Node + 48 Rust خضراء؛ بناءٌ إنتاجيّ موقّع مع أصول المحدِّث (DMG + `.app.tar.gz` + `.sig` + `latest.json`).

### v8.1.0

أول إصدارٍ عامٍّ مُصقول لهوية «نَسَق / شَذْب» الجديدة: صقلٌ بصريّ شامل، وفتحٌ خارجيّ لرابط المشروع، ومستودعٌ جاهز للإنتاج. Minor لأنه تحسيناتٌ داخل تجربةٍ قائمة — بلا تغيير في المحرّكات أو العقود أو قواعد المعالجة، وعزل البرجين أخضر.

- **الهوية البصرية الجديدة مكتملة**: محورا `data-mode` (نَسَق بنفسجيّ / شَذْب طوبيّ) × `data-appearance` (فاتح/داكن/تلقائي) على نظام توكنز دلاليّ واحد، بلوحاتٍ كاملة لكل برجٍ في المظهرين. الرأس والإعدادات و«حول» والأيقونة والألوان تتبدّل كوحدةٍ واحدة عبر خريطة هويةٍ مُلزمة، فلا حالة هويةٍ مختلطة.
- **إعادة تصميم صفحة «حول»**: هوية المنتج أولًا (أيقونة، اسم، وصف)، ثم بطاقة بيانات (الإصدار · المنصة macOS·Windows · المحرّك Rust + Tauri)، ثم التحديثات، فرابط **صفحة المشروع** بأيقونة GitHub الرسمية، ففاصلٌ هادئ، وخاتمةً حقوق سلطان بالمونوغرام المعتمد — بتراتبٍ يجعل المنتج أساسًا وحقوق المطوّر توقيعًا هادئًا.
- **تحسينات الطباعة**: الأوزان المعتمدة محليًّا بلا CDN — Almarai (400/700/**800** لاسم المنتج) للواجهة، وCairo (400/500) للمتن والمساند؛ كل قيم الأوزان تطابق وجوهًا حقيقية مع `font-synthesis: none`، ولا تباعد أحرف على العربية.
- **تحسين أيقونة التطبيق**: مربّعٌ مستدير معتمد بزوايا **شفافة** (شكل macOS الصحيح) وحبرٍ داكن ورمزٍ بحجمٍ بصريّ متوازن — عولج به «الحشو الأبيض» و«صِغَر الرمز» معًا، وأُعيد توليد المجموعة كاملة (‏icns من 16 إلى 1024) مع تحقّقٍ بكسليّ.
- **صقل الإعدادات**: نافذة الإعدادات ترث توكنز البرج الفعّال بالكامل (نَسَق داخل نَسَق، شَذْب داخل شَذْب) في المجموعات الست، دون طبقةٍ محايدة تخلط الهويّتين.
- **صقل المحرّر البصريّ**: إزالة حلقة التركيز البنفسجية/البرتقالية حول سطحي الكتابة والإخراج — المؤشر وتباين السطح والفواصل تحدّ منطقة الكتابة، مع بقاء حلقة التركيز على الأزرار والتبويبات والحقول والقوائم.
- **فتح «صفحة المشروع» في المتصفح الخارجي**: عبر مُشغّل Tauri الرسمي (`tauri-plugin-opener`) بصلاحيةٍ مقيّدة بنطاق `github.com` — يفتح المستودع في المتصفح الافتراضي دون مغادرة نافذة نَسَق. في وضع الويب المؤطّر يعمل الرابط بسلوكه الطبيعي.
- **تحسينات المستودع وREADME**: README مُعاد كتابته بلقطاتٍ حقيقيةٍ للإصدار الحالي (نَسَق فاتح/داكن، وشَذْب)، بأقسامٍ دقيقة (نظرة عامة، نَسَق، شَذْب، التثبيت، التطوير، التحديثات، المبادئ، الحقوق)، وحذف اللقطات والإشارات القديمة والشارات المتقادمة.
- **اتساقٌ بصريّ وصقلٌ للإنتاج**: مراجعة التباين والمحاذاة والكثافة والإيقاع عبر المظاهر والبرجين والعرض الأدنى، بلا أخطاء Console أو أصولٍ مفقودة.
- توحيد الإصدار **8.1.0** في `package.json` و`Cargo.toml` و`tauri.conf.json` وصفحة «حول» والتذييل. الاختبارات: 93 في Node و48 في Rust — كلها خضراء؛ وبناءٌ إنتاجيّ كامل (`Nasaq_8.1.0_aarch64.dmg`)، وتحقّقٌ بصريّ للحالات على الحزمة الحيّة.

### v8.0.1

جولة إصلاحات ما قبل الاعتماد على هوية v8.0.0 — اتساق الهوية والحالة، وتصحيح أيقونة التطبيق، واعتماد نظام حقوق سلطان الرسمي. لا تغيير في المحرّكات أو العقود أو السلوك الوظيفي.

- **أيقونة التطبيق fullbleed**: كان مصدر الأيقونة نسخة عرض بلوح أبيض معتم `(255,255,255)` حول المربّع المستدير، فبدا الرمز مصغّرًا داخل حاويته في كل المقاسات. استُبدل بالأصل الصحيح من حزمة الهوية (`nasaq-icon-lines-dark-new-fullbleed-1024`) الذي يملأ اللوح حدًّا إلى حدّ بحبر الهوية `#14131A`، وأُعيد توليد المجموعة كاملة (icns بمقاساته العشرة 16→1024 + ico + PNG) مع تحقّق بكسليّ لكل مقاس، وصُحّحت أيقونتا الدوك (فاتحة/داكنة) بنسختي fullbleed، وحُذفت نسختا SVG المؤطّرتان المضلِّلتان من مصادر الأيقونات. تحقّق نِيتف: Finder وGet Info والدوك والتطبيق المبني وDMG.
- **نافذة الإعدادات ترث البرج الفعّال بالكامل**: أُزيل نظام «المحايد» (`--neutral-*`) الذي كان يصبغ اللوحة بأسطح بنفسجية ثابتة فيخلط هوية نَسَق داخل شَذْب. `data-mode` على الجذر هو مصدر الحقيقة الوحيد: فُتحت اللوحة من نَسَق فكلها بنفسج نَسَق، ومن شَذْب فكلها حُمرة شَذْب — التبويب النشط، زر «حفظ»، الحقول، مبدّل المظهر، الرسائل، وحلقات التركيز. تحقّق على المجموعات الست (نَسَق/شَذْب × فاتح/داكن/تلقائي).
- **هوية الرأس تتبدّل كوحدة واحدة**: إصلاح خلل الحالة الذي أبقى «نَسَق | البوابة الأخيرة قبل النشر» ظاهرَين وشَذْب فعّال. خريطة هوية واحدة مُلزمة (`PRODUCT_IDENTITY` في `shell.js`) تراقب `data-mode` وتبدّل الاسم والعبارة وعنوان المستند معًا، مع العلامة والألوان من السمة نفسها عبر CSS — لا شروط متفرقة. عبارة شَذْب من ملفات الهوية المرجعية: «وظيفة تنقية الشذرات داخل نَسَق». هوية «حول» (العلامة والاسم والعبارة) تتبع الآلية نفسها.
- **حقوق سلطان بنظامه الرسمي في «حول»**: استُبدلت المخطوطة الكبيرة وسطر «برمجة وتصميم» بالتركيب المعتمد في `rights-system.md`: العلامة المصغرة (المونوغرام الرسمي كما هو، تلوينًا فقط) بجوار الصيغة الكاملة «صُمّم وطُوّر بواسطة سلطان» وتحتها «Sultan — Design & Development» أصغر. لون العلامة `--sultan-mark` من توكنز هوية سلطان حصرًا (نهارًا `#985333` وليلًا `#D2916C`) ولا يتبع ألوان البرجين أبدًا.
- **التذييل المهيكل**: فُكّت الجملة المدموجة إلى عناصر مستقلة: «لا نكتب عنك أبدًا» · فاصل · مونوغرام سلطان المصغّر + «من أعمال سلطان» (الصيغة المختصرة الرسمية) · الإصدار `v8.0.1` عنصر بيانات منفصل نحويًا وبصريًا بفاصل شعري — هادئ وثانوي.
- الاختبارات: 93 Node + 48 Rust خضراء؛ بناء إنتاجي كامل (`Nasaq_8.0.1_aarch64.dmg`)؛ تحقّق متصفحي على المجموعات الست ونِيتف على الحزمة المبنية — بلا أخطاء Console وبلا أصول مفقودة.

### v8.0.0

انتقال بصريّ كامل إلى هوية «نَسَق / شَذْب» الجديدة، وحذف نظام الهوية السابق بالكامل — Major لأن الواجهة تبدّلت كلّيًّا. المحرّكات وعقود JSON وقواعد المعالجة النصية لم تُمَسّ، وفصل `framed`/`native` باقٍ كما هو، واختبار العزل بين البرجين أخضر.

- **نظام متغيّرات دلالي جديد على محورين فقط** (`src/styles.css`): `data-mode` للبرج الفعّال (غيابه = نَسَق بنفسجيّ، و`shadhb` = شَذْب طوبيّ أحمر) × `data-appearance` لليل. أربع لوحات كاملة (نَسَق/شَذْب × فاتح/داكن) بقيم مرجع الهوية حرفيًا، والمشتقّات (الحدود/الصبغات/الظلال) تُحسب من الجذور فتتبع اللوحة الفعّالة. حُذفت العائلات اللونية الست وكتلها الليلية كلها.
- **علامة الهوية الواحدة**: أربعة سطور مضبوطة على محور محاذاة ونقطة تصحيح — نَسَق سطرٌ ثالث متّصل، وشَذْب القصّة نفسها في الهيكل مع مِبضع مائل. مضمّنة SVG بحبر `currentColor`، ومحورها ونقطتها بلون الفعل، وتتبدّل بالبرج الفعّال عبر `data-mode`. تظهر في الشريط وفي صدر صفحة «حول». حُذفت الأيقونات الأربع القديمة (السطور/النون/النسيج/كلمة نسق).
- **المظهر: ثلاثة خيارات فقط — فاتح، داكن، تلقائي**. «تلقائي» (والافتراضي) يتبع مظهر النظام حيًّا؛ «فاتح»/«داكن» يثبّتان يدويًا. مفتاح واحد `localStorage["nasaq-appearance-choice"]` بثلاث قيم. حُذف اختيار العائلة اللونية واختيار الأيقونة من الإعدادات ومن الكود، وتُمحى مفاتيحها القديمة (`nasaq-theme`, `nasaq-logo`, `nasaq-appearance`) بأمان عند الإقلاع فلا يتعطّل التطبيق بقيمة متقادمة.
- **الخطوط بأدوارها المرجعية**: Almarai لواجهة التحكّم (العناوين، الأزرار، التبويبات، أسماء الأقسام، عناصر التبديل) وCairo للنص والمحتوى (الحقول، الرسائل، الإرشادات، النصوص الطويلة، عدسة القراءة) — محليًا بلا CDN. حُذف خطّا Amiri وAref Ruqaa ورخصهما بعد استبدال استخداماتهما.
- **أيقونة التطبيق والدوك الجديدة**: أيقونة نَسَق (مربّع مستدير مستمرّ، سطور مسطّرة، محور ونقطة بنفسجية) بنسختين فاتحة/داكنة. أُعيد توليد أيقونات الحزمة في `src-tauri/icons` من المصدر الجديد (1024)، وبُسّط أمر `set_dock_icon` من أربعة تصاميم إلى تصميم واحد يتبع الوضع (فاتح/داكن). أيقونة الحزمة في Finder/Launchpad تبقى أيقونة البناء.
- **هوية المطوّر «سلطان»**: مواد الهوية والحقوق الرسمية منظّمة في `branding/sultan/` (المخطوطة الأصلية مع `integrity.json` وبصمة SHA‑256، ونسخ الشعار الملوّنة، وصيغ الحقوق، والتوكنز، والقوالب) منفصلةً عن هوية التطبيق. توقيع «حول» هو المخطوطة الرسمية نفسها (مسار محفوظ، `currentColor`، تلوينًا وتموضعًا فقط) خاتمةً هادئة تحت هوية التطبيق، وسطر الاعتماد «برمجة وتصميم: سلطان».
- **حذف الهوية السابقة بعد إثبات عدم الاستخدام**: مجلد `nasaq-brand` كاملًا، وأيقونات الدوك الثماني القديمة، وكتل CSS لبطاقات العائلة والأيقونة، وتعليقات النظام السابق. لا مجلدات `old/legacy/backup` في المنتج النهائي — تاريخ الهوية باقٍ في Git وحده.
- الاختبارات: 93 في Node خضراء (حُدّث فحص اكتمال القشرة ليطابق دالة المظهر الجديدة)، وبناء Rust نظيف؛ وتحقق متصفحي على اللوحات الأربع والمظاهر الثلاثة والتنقّل بين البرجين وصفحة «حول» بلا أخطاء Console أو أصول مفقودة.

### v7.0.0

هوية بصرية جديدة كاملة ونظام مظهر جديد — Major لأن تجربة الاستخدام تغيّرت بوضوح: أيقونات وشعار جديدان، عائلات لونية أعيد تصميم فلسفتها، ونظام وضعٍ يتبع النظام. (رقم 6.3.0 استُخدم أثناء التطوير ولم يُشحن قط — هذا أول إصدار يحمل كل ما يلي.)

- **اعتماد أصول `nasaq-brand` حصرًا**: «السطور» (الورقة المسطّرة والهامش ونقطة التصحيح — مرشَّح دليل هوية «المخطوطة الرقمية») هي أيقونة التطبيق وشعاره الافتراضيان. أيقونات الحزمة في `src-tauri/icons` من مولَّدات `AppIcon-Lines-Light`، وحُذفت الأصول القديمة كلها: `nasaq-mark*.svg` ومجلد `branding/` و`icon-source.png` — لا أصل بصري من خارج `nasaq-brand`.
- **رمز الشريط تكيفي ومبدّل أيقونة في «المظهر»**: الأيقونات الأربع (السطور، النون، النسيج، كلمة نسق) مضمّنة SVG بحبر `currentColor`، ونقطة التصحيح `#E23B2E` ثابتة بالبناء. الاختيار عبر `data-logo` يُحفَظ في `localStorage["nasaq-logo"]` ويُطبَّق مبكرًا منعًا للوميض، وأي قيمة مجهولة تُطبَّع إلى «السطور».
- **أيقونة دوك فعلية وقت التشغيل**: أمر Rust جديد `set_dock_icon` (`objc2-app-kit`: `NSApplication.setApplicationIconImage` على الخيط الرئيس) يبدّل صورة الدوك إلى التصميم المختار بنسخته الفاتحة/الداكنة حسب الوضع الفعّال، عند الإقلاع ومع كل تبديل — الصور الثماني مضمّنة في الثنائي (`src-tauri/icons/dock`، قالب أبل من `nasaq-brand`). أيقونة الحزمة في Finder/التطبيقات/Launchpad تبقى أيقونة البناء (حدّ منصة: لا إعادة كتابة لحزمة موقّعة).
- **ست عائلات لونية**: ورق نَسَق، نحاس، زيتون، حبر أزرق، ورد عتيق، رماد ناعم. نهارًا ورق القراءة واحد محايد (المظهر الفاتح الأصلي) وشخصية العائلة في اللهجات المضبوطة وحدها — الفعل الرئيس والعنوان وحلقة التركيز وصبغة التحويم والحالات المفعّلة؛ وليلًا لكل عائلة نسختها الكاملة الخاصة: امتداد داكن لهويتها لا سواد مشترك. ألوان الحالات (الخطأ والحُمرة والزيتوني) ثابتة عبر العائلات: حالة لا هوية. المعرّفات التقنية القديمة أُبقيت حفاظًا على الاختيار المحفوظ.
- **لوحة إعدادات محايدة معتمة في الوضعين**: رموز `--neutral-*` (نهارًا الورق الكريمي الأصلي، وليلًا فحمي لالوني `#212121/#191919`) لا تعرّفها كتل العائلات فلا يتسرب نحاس أو ورد أو زيتون أو أزرق إلى اللوحة وحقولها وبطاقات المعاينة وحبر أيقوناتها — العائلة داخلها في العيّنات وحدود المختار وعلامته والتبويب النشط وزر «حفظ» فقط. غشاء اللوحات فحمي محايد لا يُشتق من ظل العائلة.
- **نظام الوضع الجديد**: مبدّل شمس/قمر داخل «المظهر» هو أداة التحكم اليدوية الوحيدة (زر قمر الشريط أُزيل). قبل أول اختيار يدوي يتبع التطبيق مظهر النظام حيًّا (`prefers-color-scheme` مع مستمع `change`)، وبعده يثبت على المحفوظ في `localStorage["nasaq-appearance-choice"]`. المفتاح القديم `nasaq-appearance` كان يُكتب تلقائيًا كل إقلاع ففرض ليلًا بائتًا نجا حتى من إعادة التثبيت — يُتجاهل ويُمحى عند الإقلاع.
- **أمن واعتمادات**: ترقية `plist 1.9→1.10` تسحب `quick-xml 0.41` وتسد ثغرتين عاليتي الخطورة (RUSTSEC-2026-0194/0195)؛ `npm audit` صفر ثغرات؛ تدقيق كامل للأصول والمعرّفات والأنماط الميتة وحقن DOM وتسجيل الأسرار — نظيف. تحذيرات RUSTSEC الباقية كلها لحزم GTK الخاصة بلينكس ولا تدخل منتج macOS.
- **الخطوط والبنية كما هي**: Cairo للواجهة وAlmarai للمتن محليًا بلا CDN، والتكوين والمسافات ثابتة.
- الاختبارات: 93 في Node و48 في Rust كلها خضراء؛ تحقق متصفحي للحالات الاثنتي عشرة (٦ عائلات × وضعين)؛ وتحقق على الحزمة الحقيقية: اتباع النظام من حالة نظيفة، تنظيف المفتاح الموروث، ثبات الاختيار اليدوي والأيقونة بعد إعادة التشغيل، وتبديل الدوك الحي بالنسختين — بلقطات من الجهاز نفسه.

### v6.2.0

- **إصلاحات تدقيق v6.1.0** (التقرير في `fablr-5-nasaqV6.2.2A.md`): حارس اختصار ⌘+Enter — لا يعمل إلا حين يكون زر «نسق» ظاهرًا فلا ينطلق نداء خفي من وضع آخر (١-أ)؛ علم انشغال مشترك بين «نسق» و«سطور أقل/أكثر» يمنع سباق نداءين على النتيجة (٢-أ)؛ تفعيل سياسة أمن المحتوى CSP بعد أن كانت معطلة (٣-أ)؛ «فصل الجمل» صار يكسر بعد ؟ و! و… لا النقطة وحدها، مع بقاء استثناءي 3.14 وتتابع النقاط (٢-ب)؛ رسالة صريحة لنفاد رصيد المزوّد 402 (٢-ج)؛ المسودات القديمة بمنصة «سابستاك» تستعيد أدوات سابستاك (٢-و)؛ رسالة الاستيراد تصدُق بعد قصّ السقف (٢-د)؛ drafts.json التالف يُنحّى `drafts.json.corrupt` بدل محوه (٢-ز)؛ عدّاد العرض يعدّ نقاط الترميز — الإيموجي محرف واحد (١-د)؛ وتنظيف التعليقات البائتة (إحالات main.js/main.rs وأرقام v6.2/v6.3 وتعليقا «لا واجهة بعد») (٤-أ، ١-هـ).
- **إعادة تقسيم الواجهة على مرجع التصميم المعتمد**: صفوف بعرض النافذة تفصلها خطوط رفيعة (شريط علوي بشعار «البوابة الأخيرة قبل النشر»، صف محاور، عمودان، توقيع)؛ عمود الإدخال يمينًا بعرض 44% وفاصل رأسي رفيع؛ الخانات على الورق مباشرة بلا صناديق؛ زر «مسوداتي»؛ عنوان النتيجة صار «النتيجة» و«ملاحظات التنسيق» صارت «ما تغيّر» بصندوق قابل للطي بشيفرون؛ حالة الفراغ علامة «نَسَق» مائية وتلميح؛ التذييل «لا نكتب عنك أبدًا».
- **قائمة منسدلة بهوية المخطوطة** (جزء من الهوية): زر بحدّ رفيع وشيفرون، ولوحة ورقية بظل ناعم بندها المختار أدكن وأثقل — مبنية فوق `<select>` القائم (المعرّفات والقيم والقراءة البرمجية كما هي حرفيًا).
- **أدوات النتيجة أيقونات**: الأزرار الثمانية (تراجع، سطور أكثر/أقل، فصل الجمل، حذف الفراغات، حفظ كمسودة، عدسة القراءة، نسخ) صارت مجموعة مقسّمة واحدة بأيقونات SVG صغيرة بتلميحاتها القديمة و`aria-label` — المعرّفات والمنطق بلا تغيير، و«تصدير لسابستاك» يبقى نصيًا داخل المجموعة.
- **تحسينات وصول وتباين**: قاعدة `:focus-visible` معدنية عامة وحلقة داخلية لخانات النص (١-ج)؛ ذهب العنوان عُمّق إلى ≥4.5:1 على الورق (١-و)؛ ارتفاع موحّد 32px لصف المحاور (١-ب).
- **ألوان شذب الليلية (ش-٣ — لون فقط، التجميد السلوكي قائم)**: نسخ ليلية للحُمرة والزيتوني وتلميح العهد (صار متغيرًا `--covenant-ink` بدل لون مقسوّ) تبلغ كلها ≥4.5:1، وتعميق الزيتوني الفاتح. حشوات دمجٍ حاوية فقط لعمود شذب بعد زوال حشوة الجسد — لا مساس بأي منطق أو نص أو بنية في الوضع المجمَّد، وبنود ش-١/ش-٢/ش-٤/ش-٥ باقية مسجَّلة لما بعد التجربة.
- **الخطوط كما هي**: Cairo للواجهة وAlmarai للمتن — محليًا بلا CDN، بلا أي تغيير.
- الاختبارات: 93 في Node (ثلاثة جديدة لفصل الجمل) و48 في Rust — كلها خضراء، وتحقق متصفحي كامل للوضعين والمظهرين والنافذة الضيقة.

### v6.1.0

- **تقسيمة الواجهة طابقت مرجع Figma**: الشريط العلوي بثلاث مجموعات (طرفان مرنان يوسّطان مبدّل نَسَق/شَذْب)، الفعل الرئيس «نسّق النص» صار «نسق» ومضبوطًا بارتفاع أدوات الشريط، وأدوات النتيجة (نسخ، عدسة القراءة، حفظ كمسودة، فصل الجمل...) أُعيد تجميعها ككتلة واحدة تابعة لعنوان «النص المنسّق» بدل عناصر متفرقة.
- **وضع ليلي جديد**: زر قمر صغير بجانب الإعدادات يبدّل طبقة داكنة مستقلة عن عائلة الثيم (`data-appearance` لا `data-theme`) — تعتّم الخلفية والنص فقط، وتبقي عائلة المعدن المختارة (ذهب/نحاس/برونز...) كما هي. علامة «نسق» في الشريط لها توأم بالألوان الداكنة نفسها فلا تبقى ملصقًا فاتحًا فوق الخلفية الليلية.
- **لا تغيير سلوكيًا**: تعديلات عرض (CSS/markup) وواجهة فقط — منطق نسق وشَذْب وبرومپت شَذْب ونتيجة الإخراج كما هي حرفيًا. الاختبارات: 90 في Node و48 في Rust — كلها خضراء.

### v6.0.0

- **قفزة Major — اعتماد الهوية الفاتحة**: صار مظهر التطبيق الأساسي ورقًا دافئًا فاتحًا بحبر داكن (مأخوذ من تصميم الواجهة المعتمد)، بدل الحبر الليلي الداكن. تغيّر بصري واضح في تجربة الاستخدام — لذا Major.
- **لون فقط، لا تخطيط ولا منطق**: البنية والتخطيط والمسافات وترتيب اللوحات كما هي حرفيًا؛ تغيّرت **قيم متغيرات CSS** فقط (النظام var-driven بالكامل). لا مساس ببرومپت شَذْب ولا منطق الفحص ولا نتيجة الإخراج (تجميد شَذْب محفوظ).
- **الهرمية**: المتن حبر داكن واضح (`#1c1710`)؛ العناوين الثانوية رمادي دافئ مقروء لا باهت؛ الأزرار الثانوية بحدود تجعلها تبدو قابلة للنقر.
- **الهوية محفوظة على الورق**: ذهب الناسخ لأزرار نسق الرئيسة، وحُمرة المصحِّح لأزرار شَذْب — مُعمّقان ليُقرآ على الفاتح؛ والأخضر الزيتوني للضمانة كما هو. لا ألوان جديدة.
- **الثيمات الست** أصبحت عائلات ورق فاتح تختلف بالمعدن فقط (ترث الأساس الفاتح من `:root`)، وأسماؤها حُدّثت.
- **الأيقونات** أُعيد توليدها فاتحة: «مخطوطة رقمية فاتحة» — ورق دافئ + «نسق» بحبر داكن + خطوط ذهبية + نقطة المصحِّح الحمراء، بزوايا شفّافة بلا أبيض.
- الاختبارات: 90 في Node و48 في Rust — كلها خضراء، والتحقق المتصفحي كامل للوضعين.

### v5.2.2

- إصلاح ظهور «أدوات التجربة»: كانت تعتمد على تغليف `presentResult` وحده، وهو مسار لا ينادَى إنتاجيًا (زر «افحص الشذرة» يستدعي مرجعًا محليًا داخل شَذْب.js). الكتلة الآن تظهر بمراقبة `#cuts-list` مباشرة — يغطي كل نتيجة فحص مكتملة (قصّات، أو حالتا الفراغ).
- إضافة زر «نسخ» صغير وهادئ تابع لصندوق «بعد التشذيب — من نصّك فقط»، ينسخ النص المشذَّب فقط دون مسّ الحالة أو الإرسال.
- لا تغيير في برومپت شَذْب أو منطق الفحص أو نتيجة الإخراج.

### v5.2.1

- إضافة سجلّ تجربة شَذْب كطبقة مؤقتة مستقلة.
- حفظ التجارب داخل `data/shathb-trials/shathb-trial-log-vN.json`.
- لا تغيير في برومپت شَذْب أو منطق الفحص أو نتيجة الإخراج.

### v5.2.0

- **هوية بصرية جديدة (المرحلة 6): «مخطوطة رقمية»** — اعتُمدت من أربعة اتجاهات صُمِّمت في Figma: النص على الورق المسطَّر بخط هامش، وكلمة «نسق» بذهب الناسخ، والنقطة الحُمرة علامة المُصحِّح الصامت — الهوية الوحيدة التي تحمل غرفتَي التطبيق معًا (الناسخ يُذهِّب والمُصحِّح يعلِّم بالحُمرة).
- **الطقم كاملًا**: أيقونة macOS **ممتلئة (full-bleed)** — الحبر يملأ المربّع حتى الحواف بزوايا مستديرة، بلا هامش أبيض ولا صفيحة خلفية ولا ظل ولا حدّ (هوامش آبل القديمة كانت تُحدث صفيحة بيضاء في macOS الحديث فأُزيلت) + علامة الشريط داخل الواجهة + الشعار الأفقي. نسخة واحدة تُشتقّ منها كل أحجام `src-tauri/icons` والـicns. الزوايا شفّافة حقيقيةً (لا بيضاء).
- **الكلمات مسارات SVG لا نصوص**: «نسق» (Cairo وزن 900) و«نَسَق» (وزن 800) مستخرجتان من خط التطبيق نفسه بتشكيل عربي صحيح عبر HarfBuzz — لا اعتماد على خطوط النظام، والهوية بلوحة ثيم v5.1 حرفيًا (`#12100C`/`#D8B45F`/`#D96F52`).
- تحديث `branding/` بالكامل (المصادر، iconset، icns، تصديرات PNG) وتوثيق الهوية في `branding/README.md`.
- **لا تغيير سلوكيًا**: الواجهة والعقود والنواة والاختبارات كما هي (90 Node و48 Rust خضراء) — أصول فقط، وتجميد شَذْب قائم.

### v5.1.0

- **صقل واجهة فقط (المرحلة 5)** بمرجع تصميم Figma كموجّه بصري: لا تغيير في أي عقد أو نداء نموذج أو منطق تحقق أو تخزين — نواة نسق وشَذْب بلا حرف تعديل، واختبارات Rust الـ48 كما هي.
- **الثيم الافتراضي أدفأ وأضوأ** («مكتب كاتب ليلي بضوء مصباح»): الذهب `#d8b45f`، الورق `#f1e8d8`، وحُمرة المصحِّح `#d96f52` — الثيمات الخمس الأخرى لم تُمس. صبغتا تفعيل جديدتان (`--accent-tint`/`--prune-tint`) مشتقتان من معدن الثيم فتسريان على العائلات الست تلقائيًا.
- **المبدّل «نَسَق | شَذْب» أعيد بناؤه**: حاوية غائرة بحشوة داخلية، كل زر بزواياه، والتفعيل صبغة وحد رقيق بدل التعبئة الصلبة — زال الكسر البصري وخط القص عند الزوايا.
- **«افحص الشذرة» انتقل** إلى شريط سفلي في لوحة النص الخام (يظهر في وضع شَذْب حصرًا عبر `data-mode`) وسطر العهد فوقه — الفعل صار ملاصقًا لمنطقة الكتابة، وحجمه قرين «نسّق النص».
- **تذييل ثابت لنتائج شَذْب**: شريط الضمانة (بخلفية زيتونية صبغية) وزر «أرسل إلى نَسَق» (بهوية الذهب — الوجهة غرفة الناسخ) خارج منطقة التمرير فلا يُقصّان مع النص الطويل. منطق الجسر وشرطا الأمان كما هما حرفيًا.
- **حالتا فراغ القصّات افترقتا**: «لم يجد شَذْب موضع حذف آمن.» حين لا مقترحات أصلًا، و«لم يبقَ اقتراح حذف صالح بعد التحقق.» بصندوق كهرماني يسمّي العدد والسبب حين أسقط التحققُ كل المقترحات — وعبارة «الشذرة مُحكَمة كما هي» حُذفت (كانت تدّعي حكمًا سببه الفعلي فشل التحقق لا إحكام النص).
- **«بعد التشذيب — من نصّك فقط»** لا يظهر إلا بعد حذف فعلي — قبله كان نسخة مطابقة للأصل بلا معلومة.
- **بطاقات القصّ**: المعلّقة بصبغة الحُمرة والمحسومة تخفت وتتحيّد؛ الاقتباس بخط المتن (Almarai) مشطوبًا بحُمرة خافتة (البطاقة معاينة حذف)، ويعود سليمًا بلا شطب عند «أبقِ»؛ وزرا القرار زوجان بعرض متساوٍ — «أبقِ» يمينًا محترمًا و«احذف» صبغة وحدّ لا تعبئة عدوانية.
- **تباعد وأهداف نقر**: أزرار الأدوات أرحب (4px×10px بخط 12px)، حشوة سفلية أوسع للجسم وسطر التوقيع، رؤوس اللوحات تلتف بدل أن تُقص في النوافذ الضيقة، والتلميحات بوزن Cairo 300.
- **وعد مفتاح الإطفاء أعيدت صياغته**: الإطفاء يخفي شَذْب كليًا (المبدّل ولوحة شَذْب وشريط الفحص) وتبقى نَسَق وحدها ظاهرة — لم نعد نَعِد بمطابقة حرفية لواجهة إصدار سابق لأن نَسَق نفسها تُصقل.
- حرّاس جدد في اختبارات العزل: الصياغتان الجديدتان حاضرتان والقديمة زالت، شرط الحذف الفعلي للمعاينة، شريط الفحص خلف `data-mode` وفي لوحة الإدخال، والضمانة والجسر في التذييل خارج جسد التمرير. الاختبارات: 90 في Node و48 في Rust — كلها خضراء.

### v5.0.0

- **قفزة Major**: أول ظهور لوضعٍ ثانٍ في الواجهة — تجربة الاستخدام لم تعد وضعًا واحدًا، وهذا معيار الترقيم من الآن (انظر نظام الترقيم أعلاه).
- **واجهة «شَذْب» (المرحلة 4 — MVP)**: المبدّل «نَسَق | شَذْب» في الشريط، بحُمرة المصحِّح على هوية المخطوطة نفسها (الناسخ يُذهِّب والمصحِّح يعلّم بالحُمرة) — الثيمات الست والخلفيات كما هي، والحُمرة إشارة وضع لا ثيمًا.
- **الفحص**: «افحص الشذرة» ينادي `prune_text` مرة واحدة ويعرض بطاقة القراءة والقصّات المقترحة وعدّاد ما أسقطه التحقق الآلي بصيغة هادئة — ولا يُعرض أي نص كامل من النموذج أبدًا.
- **قرار الكاتب لكل قصّة**: «احذف» يطبّق محليًا عبر مقص prune.js نفسه (قصّ حرفي + ترتيب آثار، بلا أي نداء نموذج بعد الفحص) ويحدّث معاينة «بعد التشذيب» فورًا؛ و«أبقِ» يعلّم القصّة متروكة بلا مساس؛ والاقتباس الذي أزاح موضعَه قصٌّ سابق يُعلَّم «تعذّرت» بلا اجتهاد.
- **شريط الضمانة الحي**: بعد كل قرار تُعاد الشهادة (`verifySubset`): لم تُضف كلمة — كل الناتج من نصّك، مع عدد الكلمات المحذوفة؛ وخرقها (لا يقع بالبناء) يُظهر الخرق بالاسم ويحجب الإرسال.
- **الجسر الوحيد**: «أرسل إلى نَسَق» يمرّر النص المشذَّب عبر `sendToNasaq` في القشرة حصرًا — يصير خامًا لنسق بجلسة جديدة (حدث الإدخال نفسه)، ويعود الوضع لنسق. حارس يمنع الكتابة فوق تعديل يدوي: إن تغيّرت الخانة بعد الفحص يُطلب فحص جديد. لا حفظ في المسودات من شَذْب — الحفظ من جهة نسق وحدها.
- **مفتاح الإطفاء يعمل بالاتجاهين**: `localStorage["nasaq-shadhb"] = "off"` يخفي المبدّل والوضع كليًا فتعود الواجهة مطابقة لـ v4.3 حرفيًا (مُتحقَّق منه في المتصفح)، والافتراضي مفعَّل.
- **حماية نسق**: عقوده وأوامره ورسائله بلا حرف تغيير (nasaq.js وبرج nasaq في النواة لم يُمسّا)، وتعديلا القشرة محصوران في الجسر والمفتاح. الحراس تحدثوا: نداء نواة وحيد مسموح لشَذْب (`prune_text`)، لا لمس لدوال نسق بالاسم، ولا كتابة في الخانة إلا عبر الجسر.
- الاختبارات: 86 في Node و48 في Rust — كلها خضراء، وتحقق متصفحي كامل: الإطفاء يخفي كل شيء، والخط كاملًا يعمل (فحص ← احذف/أبقِ ← ضمانة ← إرسال)، ونسق يعمل بعد الجسر كما قبله (تنظيف/فصل/تراجع)، والتبديل ذهابًا وإيابًا لا يفقد حالة أي وضع، ولا خطأ console واحد.

### v4.3

- **نواة «شَذْب» الخلفية (المرحلة 3)** — بعد معايرة العقد يدويًا: لا واجهة ولا مبدّل ولا زر، وتجربة نسق كما هي حرفيًا.
- **معايرة العقد على سبع شذرات حقيقية** من نصوص الكاتب قبل أي كود: الجولة الأولى — حرفية الاقتباسات 7/7، قائمة فارغة مستحَقة لأربع شذرات مُحكَمة، لا نص كامل ولا بدائل ولا «تحسين لغة»؛ والعلّة الوحيدة قصّات أكبر من اللازم (بتر 52 كلمة من شذرة 89). الجولة الثانية بقاعدة «أصغر قصٍّ يحقق الأثر» أصلحت أغلبها (قصّتا 3 و4 كلمات) وبقي تجاوز واحد (21 كلمة) — فتقرر أن السقف يُفرض آليًا لا التماسًا.
- **برج `shadhb/` النووي**: `contracts.rs` العقد المعايَر (حرارة جراحية 0.3، تفكير محدود 1024 — قرارا برجٍ لا إعدادان، وصفر نسق المقفول لا يُمس)؛ `verify.rs` التحقق الآلي المطابق سلوكيًا لوحدة الواجهة prune.js (قصّ حرفي، ترتيب آثار، شهادة الناتج ⊂ الأصل، وسقف حجم القصّة: 14 كلمة ونصف النص — البوابة تصدّ الفاحش والعقد يطلب المثالي)؛ `commands.rs` أمر `prune_text` بمخرج صارم: بطاقة قراءة + قصّات (لكلٍّ اقتباس وسبب وعدد كلمات يُحسب محليًا وأثر متوقع وعلامة أمان) + عدّاد المُسقَط آليًا. **الضمانة بنيوية**: لا حقل نص كامل في بنية النتيجة أصلًا — «النسخة المحسّنة» يستحيل تسريبها.
- **حالات مشتركة بين النواة والواجهة** (`tests/fixtures/prune-cases.json`): ملف واحد يستهلكه الطرفان — Node عبر `prune-fixtures.test.js` وRust بالتضمين وقت الترجمة — فأي انحراف بين التطبيقين يفشل الاختباران معًا.
- **سدّ فجوة تكلفة على OpenRouter** كشفتها المعايرة: تعطيل التفكير كان يسري على Gemini المباشر فقط، والمزود الحالي OpenRouter يشغّل التفكير الديناميكي بصمت. النقل المشترك صار يرسل حقل `reasoning` لـ OpenRouter (تعطيل عند الصفر، سقف فوق ذلك) في كل محاولة، وميزانية التفكير معلمة يقررها البرج: نسق صفر مقفول (`FORMAT_THINKING_BUDGET` ثابت صلب مختبَر)، وشَذْب ميزانيته في عقوده.
- **حراس تجميد**: اختبار يثبّت عقود نسق حرفيًا (العقد، عقد الأسطر، الحرارتان، جمل النظام المؤسِّسة) بعد بناء النواة؛ وحارس عزل في Node يفحص مصادر الأبراج الثلاثة — لا استيراد متبادل ولا تسرب ثوابت ولا عقد في الأساس المشترك.
- الاختبارات: 48 في Rust (منها 12 لنواة شَذْب) و85 في Node (منها 17 للحالات المشتركة) — كلها خضراء بلا تحذير، والبناء سليم.

### v4.2

- **العزل الأمامي (المرحلة 2)**: تكرار عزل النواة في الواجهة دون أي تغيير في تجربة نسق — لا مبدّل مرئي ولا زر جديد ولا مساس بالعقود أو سلوك التنسيق، والواجهة تبدو وتعمل مثل v4.1 تمامًا.
- **main.js انقسم نقلًا بايت-حرفي** (بالنطاقات لا بإعادة الكتابة) إلى: `shell.js` قشرة مشتركة لا تعرف وضعًا (النواة، الإعدادات، الثيمات، المسودات تخزينًا وعرضًا، الحافظة، الرسائل، العدّاد)، و`nasaq.js` وحدة وضع التنسيق كاملة (المحاور، الجلسة، التراجع، الأدوات، التنويعات، العدسة). الوضع يصل نفسه بالقشرة عبر التسجيل حصرًا: مفردات عرض المسودات (النوع وترتيبه)، معالج الاستعادة، الإيداع التخزيني لزر «حفظ»، ومُغلقات Escape بالأولوية — القشرة لا تنادي دالة وضعٍ باسمها أبدًا.
- **هيكل «شَذْب» خامد**: `shadhb.js` يُحمَّل أخيرًا وكل جسده داخل درع — فشله الكامل لا يمس نسق؛ خلف مفتاح `shadhbEnabled=false` في القشرة فلا أثر مرئيًا ولا سلوكيًا له. لا واجهة ولا عقد ولا نداء نموذج — حجز بنية فقط.
- **`prune.js` — مقص شَذْب النقي** (بلا نموذج وبلا DOM): `applyCuts` حذف حرفي لا اجتهاد فيه (الاقتباس غير الموجود حرفيًا يُتخطى ويُبلَّغ)، و`tidyAfterCut` ترتيب حتمي لآثار الحذف لا يضيف محرف كلمة، و`verifySubset` شهادة العهد — الناتج ⊂ الأصل كتتابع كلمات، فالإضافة وإعادة الترتيب تُسقطان التحقق آليًا. 16 اختبار Node تغطيها وخط الأنابيب كاملًا.
- **اختبارات حارسة للعزل** (`tests/isolation.test.js`): ترتيب التحميل عقدٌ مفحوص، لا نداء نموذج في القشرة، لا إشارة لشذب في نسق، خمود الهيكل ودرعه وبوابته، لا إعلانات عليا مكررة بين الملفين، واكتمال دوال الوضع والقشرة بعد الانقسام — أي انزلاق مستقبلي في قواعد العزل يفشل هنا قبل أن يصل الواجهة.
- الاختبارات: 67 في Node (منها 16 للمقص و9 حارسة) و33 في Rust — كلها خضراء، والبناء سليم.

### v4.1

- **تحصين قبل «شَذْب»**: إصدار أساسٍ بلا ميزة نموذج جديدة — إصلاح تكلفة، وأدوات أمان للنتيجة والمسودات، وبنية عزل تمهيدية لوضع التشذيب المستقبلي دون بنائه.
- أُغلقت ثغرة تكلفة صامتة: عند رفض المزود للطلب الأول (400/422) كانت إعادة المحاولة تُسقط تعطيل تفكير Gemini فيعمل التفكير الديناميكي بصمت (أبطأ وأغلى بأضعاف). الآن `thinking_budget: 0` يُرسل في كل محاولة بلا استثناء — `response_format` وحده يسقط عند الإعادة، واختبار يضمن الصفر لأي نموذج وأي محاولة.
- رسائل النداءات الثلاثة أعيد ترتيبها: الثابت أولًا (القواعد والعقود وتعليمات الصيغة)، ثم المتغير (التوجيهات فرقم المحاولة)، والنص أخيرًا — الجمل نفسها حرفيًا بلا أي تغيير معنى (اختبار يثبت بقاء كل جملة)، والبادئة المتطابقة الأطول تلتقط خصم التخزين المؤقت الضمني عند المزود في الضغطات المتكررة.
- **تراجع**: زر على النتيجة يستعيد آخر صورة قبل أي تعديل (نسّق من جديد، سطور أقل/أكثر، حذف الفراغات، فصل الجمل، اعتماد منعطف أو تنويعة) حتى ٢٠ خطوة — محلي بالكامل، يستعيد المحاور والبصمة مع النص، ويُصفَّر مع بدء نص جديد.
- **نسخة احتياطية للمسودات**: «تصدير نسخة» يكتب drafts.json كما هو إلى مجلد التنزيلات باسم مؤرّخ، و«استيراد» يدمج نسخة محفوظة (حتى القديمة المسطّحة — تمر بالترحيل نفسه) دمجًا لا استبدالًا: المطابق يُتخطى، واستيراد النسخة نفسها مرتين لا يكرر شيئًا، وفشل القراءة أو الحفظ لا يمس المحفوظ.
- **بحث في المسودات**: حقل في اللوحة يرشّح بالمطابقة على النص الأصلي والصيغ بعد تطبيع الفراغات (يجد العبارة عبر كسور الأسطر) — ترشيح عرض فقط، البنية المخزنة والشارة لا تتغيران.
- **عدّاد واعٍ بالمنصة**: عند تنسيق منصة لها سقف (إكس ٢٨٠ لأطول مقطع لا للمجموع، ثريدز ٥٠٠، إنستغرام ٢٢٠٠) يُلحق العدّاد تحذيرًا هادئًا عند التجاوز — تقريبي وأمين للعربية، لا نافذة ولا منع.
- **بنية عزل تمهيدية**: main.rs انقسم نقلًا حرفيًا إلى ثلاثة أبراج — `shared/` (نقل وتخزين خام، لا يعرف عقدًا ولا prompt)، و`nasaq/` (العقود والأوامر كما هي)، و`shadhb/` وثيقة حجز فارغة تدوّن قواعد البناء يوم يُبنى. لا استيراد متبادل بين البرجين أبدًا. سلوك نسق لم يتغير، وكل الاختبارات خضراء (33 في Rust و42 في Node).

### v4.0

- **التوجيهات الخاصة**: حقل اختياري صغير (3 أسطر) تحت صندوق النص الأصلي لقرارات تنسيق موضعية لا يغطيها زر — فصل جملة بعينها، حفظ علامات الحوار، عدم لمس فقرة، عدم التحويل لهيئة شعرية… تُدمج مع النمط المختار في النداءات الإبداعية الثلاثة (نسّق، سطور أقل/أكثر، لوحة التنويعات).
- أولوية التوجيهات في تفاصيل الإخراج الموضعية فقط: قاعدة صريحة محقونة تنص أن قاعدة عدم تغيير النص تعلوها — تنسيق بحت، لا صلاحية لإعادة صياغة أو كتابة نص جديد، وما خالف ذلك من التوجيه يُتجاهل.
- الحقل فارغًا = السلوك القديم حرفيًا (الرسالة المبنية للنموذج مطابقة بايتًا لما قبل v4.0 — مختبَرة). نقلة v4.0 لأن نسق لم يعد أزرارًا فقط: صار يقبل طبقة توجيه حر دقيقة دون أن يتحول محرر كتابة.

### v3.8

- **بصمة الإيقاع**: سطر هادئ تحت النص المنسّق للمستويين الإيقاعيين (تنسيق إيقاعي/إيقاعي منقّط) — صفة من معجم محايد مغلق (هادئ/متقطّع/متدفّق/متوتّر/كثيف/متمهّل/متسارع) + جملة حسّية تصف كيف يتنفّس النص. يعود حقل JSON مستقل عن formattedText فلا يدخل النسخ أو المسودة أبدًا، ويُخفى فور أي تعديل محلي (يصف الظاهرة كما كانت لا كما صارت). أسوار حياد صريحة في العقد تمنع أي حكم أو توصية أو مقارنة بمثالي.
- **عدسة القراءة**: زر جديد يعرض النص المنسّق في لوحة بعرض هاتف نموذجي (380px) — للعرض فقط، لا تحرير، تحترم اتجاه النص الحالي (RTL/LTR)، تعمل مع أي نمط أو منصة.
- زرّا «أسطر أقل/أكثر» ومنطق التنسيق العام والشعار بلا أي تغيير.

### v3.7

- خريطة سابستاك اليدوية: على وجهي سابستاك (غير نمط الشذرة)، يضع النموذج رموزًا إرشادية للعرض فقط بين السطور — `++` (Enter، فقرة جديدة)، `**` (Shift+Enter، كسر ضيق)، و`---` (Divider) بنيوي نادر يظهر فقط عند انتقال محوري حقيقي، غيابه الأصل.
- الرموز لا تُنسخ ولا تُحفظ إطلاقًا: تُجرَّد قبل «نسخ» و«تصدير لسابستاك» وقبل أي حفظ في ذاكرة الجلسة/المسودات (نقطة اختناق واحدة تضمن الخلوّ التام)، وتُجرَّد أيضًا من «النتيجة الحالية» قبل تمريرها لـ«أسطر أقل/أكثر» فلا تتشوّه.
- ملاحظة «إرشادات سابستاك» تظهر داخل قسم «ملاحظات التنسيق» الموجود أصلًا (لا زر جديد) حين تُطلب الرموز، وتغيب تمامًا خارج وجهي سابستاك أو مع نمط الشذرة.
- زرّا «أسطر أقل/أكثر» ومنطق التنسيق العام والشعار بلا أي تغيير.

### v3.6

- زر «فصل الجمل» جديد بجانب «حذف الفراغات»: محلي حتمي بلا نموذج، يكسر السطر بعد كل نقطة تنهي جملة، ويتجاهل النقطة العشرية بين رقمين (3.14) وتتابع النقاط (...). النتيجة قابلة للحفظ كمسودة فورًا كسائر أزرار الأسطر.
- مستوى تدخل جديد «إيقاعي منقّط» بجانب «تنسيق إيقاعي»: يمر بالنموذج، ويرتكز على علامات الوقف (النقطة والفاصلة) كمواضع كسر محتملة فيختار منها ما يمثّل وقفة إيقاعية فعلية لا كل علامة آليًا، ولا يكسر نقطة داخل عدد. «تنسيق إيقاعي» الحالي بلا أي تغيير.

### v3.5

- أُصلح ضعف التنويع في لوحة سابستاك: التعليمة العامة كانت مطابقة حرفيًا بين التنويعتين ٢ و٣ (لا فرق سوى رقم المحاولة)، وعلى النصوص قصيرة الجُمل ضاق «موضع» الكسر أصلًا فتقاربت النتائج أو تطابقت.
- أمر Tauri مخصص جديد `generate_variation` (منفصل عن `format_text` تمامًا، فمسار «نسّق النص» و«سطور أقل/أكثر» لم يتأثر): كل تنويعة من الثلاث تحمل رافعة كثافة كسر مختلفة بنيويًا — الأولى تجميع (سطور أقل)، الثانية تفريد (سطور أكثر)، الثالثة وسط — لا مجرد موضع، فيظهر تباين حقيقي حتى على الجُمل القصيرة.
- قفل صريح يرافق كل تنويعة يُلزم بتطابق الكلمات والمعنى وترتيب الجُمل حرفًا بحرف بين الثلاث، والفرق الوحيد المسموح كثافة/موضع الكسر.
- حرارة مخصصة للوحة (0.92) أعلى من الحرارة الإبداعية العامة (0.85)، دون التأثير على أي نداء آخر.

### v3.4

- خط نص لوحة التنويعات صار القاهرة (Cairo، وزن 450) بدل أميري — نظيف حديث، والقاهرة مضمومة محليًا أصلًا (`--font-ui`). الأرقام (١ ٢ ٣) باقية بعارف رقعة، والزخرفة الذهبية وبقية التطبيق (الأزرار، الحقول، القوائم) بلا أي تغيير.

### v3.3

- عمق بصري بلغة المخطوطة: لكل عائلة ثيم مثلث ظل دافئ (`--shadow-rgb` — أسود دافئ/بني محروق من حرارتها)، تُشتق منه كل الظلال — لا رمادي بارد ولا زجاج ولا blur.
- الخانتان غائرتان بظل داخلي دافئ وشفة سفلية بلمحة معدن الثيم؛ القوائم والأزرار الثانوية مرفوعة بحافة علوية معدنية وظل دافئ خفيف؛ الأفعال الممتلئة بظل رفع أوضح.
- حالة التفعيل: خلفية الحقل النشط تُضاء درجة نحو الدفء (`--bg-raised`) بحد رفيع بمعدن الثيم، ومساحة المخرَج تخفت قليلًا (0.93) حين يكتب الكاتب في الخام، وcaret الكتابة بمعدن الثيم.
- تركيز تلقائي على «النص الخام» عند الفتح، وانتقالات وقار 0.2s للألوان والظلال في كل الأزرار — بلا `transform: scale` ولا حركية جوال.
- كل التأثيرات تقرأ من متغيرات الثيم (v3.2) فتتبع العائلات الست: ذهبًا ونحاسًا وبرونزًا وفضة ولؤلؤًا.

### v3.2

- أُصلح نظام الثيمات: توحيد v3.1 كان قد أذاب كتل `data-theme` فبقي المبدّل بلا أثر — أعيد وصل الثيمات بالألوان عبر متغيرات CSS: كل ثيم يعرّف عائلته فقط، والمشتقات (الحدود وألوان البطاقات) تُحسب تلقائيًا.
- ست عائلات كلها على صيغة المخطوطة (داكن دافئ + فاتح دافئ + معدن هادئ)، تختلف بحرارة المعدن ومسحة الداكن فقط: **نسق الداكن** (ذهب — المرجع)، **حبر هادئ** (داكن أعمق وذهب أخفت)، **ورق دافئ** (بنّي ونحاس)، و«ليل أزرق» و«رمل خفيف» و«رماد محايد» الباردة استُبدلت بـ**برونز** و**فضة دافئة** و**لؤلؤ عتيق** — بالمعرّفات القديمة نفسها فلا يفسد اختيار محفوظ.
- لوحة التنويعات دخلت النظام: لوحها الثابت صار متغيرات الثيم (`--card-bg/--card-text/--card-metal`)، والزخرفة الذهبية صارت قناعًا (mask) يتلوّن بمعدن الثيم النشط.
- الأزرار على قاعدة v3.1 بمعدن الثيم: الرئيسة ممتلئة والثانوية بحد معدني خافت. تباين مفحوص للثيمات الستة (الثانوي ≥ 7:1، وأدنى معدن 5.9:1).

### v3.1

- وُحّدت هوية التطبيق كله على حبر وذهب المخطوطة — هوية لوحة التنويعات نفسها بقيمها الفعلية: الحبر الليلي `#12100C` خلفيةً، الورق العتيق `#E8DFD0` نصًا، الذهب الحي `#C9A55C` تمييزًا، والحدود ذهب رفيع `rgba(201,165,92,.35)` بدل الرمادي.
- الأفعال الرئيسة («نسّق النص» و«نسخ») ذهب ممتلئ بنص داكن — تذهيب نادر يكتسب قيمته من ندرته؛ وكل الأزرار الأخرى حدّ ذهبي رفيع على داكن كأزرار اللوحة.
- النص الثانوي والتلميحات ورقٌ مخفوت دافئ (`#ABA18C`، تباين ≈ 7:1) لا رمادي بارد، والأسطح المرفوعة/الغائرة درجات دافئة من الحبر نفسه.
- توحيد لون فقط: لا تخطيط تحرّك ولا عنصر نُقل ولا بنية تغيّرت. اختيار «المظهر» باقٍ كما هو، وكل قيمه تعرض الهوية الموحّدة — لا لون قديم يعود عند تبديل الثيم.

### v3.0

- **لوحة تنويعات سابستاك**: زر «أرِني تنويعات» عند وجهة سابستاك يولّد ثلاث تنويعات دفعة واحدة (ثلاثة نداءات بعقد سابستاك نفسه وبذور محاولة متتالية) تختلف في مواضع كسر السطر وتوزيع الوقفات لا في كثافة الفراغ، وتُعرض متجاورة بتصميم حبر وذهب المخطوطة: أرقام (١ ٢ ٣) بخط الرقعة الذهبي، نص بخط أميري، وزخرفة عربية عمودية بين الأعمدة. «اعتمد هذه» يُدخل التنويعة المخرَج الرئيس فتصبح قابلة للحفظ والتصدير وتعديل الأسطر، و«ولّد ثلاثًا جديدة» يعيد التوليد ببذور جديدة. وجه النوت يُعرض يسارًا تقليدًا للمنصة وعناصر اللوحة تبقى عربية RTL.
- **تصدير لسابستاك**: زر على النتيجة (لوجهة سابستاك) ينسخها بكسور السطر المفردة محفوظة تمامًا وبلا أسطر فارغة — مطابقة لما سيظهر عند النشر (المنصة تبتلع الفراغ)، والفاصل المرئي (·) يُصدَّر كما هو لأنه محرف ينجو. العرض داخل نسق يُبقي الفراغ كراحة مراجعة.
- **بطاقة الشذرة**: حين النمط شذرة على وجهة سابستاك وفي النص منعطف (بل/لكن/غير أن/نقطتان/شرطة موجودة أصلًا)، تُعرض الصورتان — مكسورة عند المنعطف مقابل موصولة — والمستخدم يختار، والتبديل بينهما حر. الكسر وحده يُبرز الالتفاتة: لا علامة تُضاف ولا كلمة تتغير (تحوّل تخطيطي محلي خالص في `fragments.js` المختبر).
- خطان مخطوطيان جديدان مضمّنان محليًا برخصة OFL: أميري وعارف رقعة (للوحة التنويعات حصرًا — ثيمات التطبيق لم تُمسّ).

### v2.3

- حامل التنفّس لوجهة سابستاك صار كسر السطر المفرد لا السطر الفارغ — سابستاك تبتلع الفراغ عند النشر، فالعقد المحقون في كل نداء إبداعي وجهته سابستاك يوجّه النموذج لتوزيع الوقفات بمواضع الكسر. الفراغ إن ظهر يبقى في عرض نسق كراحة مراجعة فقط.
- سابستاك انقسمت وجهين تحت «تنسيق منصة»: **مقال سابستاك** (RTL يعمل، فقرات أوسع، عنوان فرعي من روح النص جائز) و**نوت سابستاك** (يفرض LTR — النص العربي ينتظم يسارًا، منشور قصير مكثّف). المسودات القديمة باسم «سابستاك» تُعامل كمقال.
- الشذرة على سابستاك: فصل الشذرات المتعددة بكسر السطر (وفي المقال يجوز الترقيم ١، ٢، ٣)، والفاصل المرئي — نقطة عارية (·) في سطر مستقل — استثناء ضيّق حين يضيع الفصل بالكسر وحده.
- «أسطر أكثر/أقل» على وجهة سابستاك يعدّلان كسور السطر لا الفراغات.
- تلميحة واحدة في الواجهة عند اختيار وجهة سابستاك تذكّر أن الفراغ يختفي عند النشر وأن الكسر هو ما ينجو (وللنوت: أنه لا يدعم RTL).
- المنصات الأخرى (إكس، ثريدز، إنستغرام، واتساب) لم تتغير.

### v2.2

- حفظ دفعي: يحتفظ نسق في ذاكرة الجلسة بآخر نتيجة لكل نوع تنسيق/منصة جرّبته على النص الحالي (كل ضغطة «نسّق» تستبدل صيغة نوعها)، وضغطة «حفظ» واحدة تودعها كلها تحت مسودتها الأمّ.
- «سطور أقل/أكثر» تحدّث آخر صيغة لنوعها (بشارة «± أسطر») ولا تنشئ مدخلًا منفصلًا. «تنظيف فقط» لا يدخل ذاكرة الجلسة.
- لا تكرار للحفظ: ما حُفظ لا يُعاد إيداعه، وضغط «حفظ» دون جديد يُعلم بلطف.
- تُصفَّر الجلسة عند تعديل كلمات النص أو استعادة مسودة؛ تغيير المسافات والأسطر وحده لا يصفّر.
- تسمية الصيغ في المسودات صارت «صيغة ١/٢/٣» (حفظات من جلسات مختلفة على النص نفسه).

### v2.1

- عرض المسودات صار هرميًا: مسودة أمّ (مقتطف النص الأصلي + عدد الصيغ) ← النوع (المستوى أو المنصة) ← «تنسيق ١/٢/٣» بترتيب وقت الحفظ.
- النص الأصلي يُعرض مرة واحدة على مستوى الأمّ ولا يتكرر داخل كل صيغة.
- الصيغ المتطابقة النصّ (بعد تطبيع داخلي للمسافات والأسطر) تتجمّع تحت أمّ واحدة؛ تغيّر الكلمات ينشئ أمًّا جديدة.
- ناتج «سطور أقل/أكثر» يُحفظ تحت نوعه الأصلي بشارة «± أسطر».
- ترحيل تلقائي للمسودات المسطّحة القديمة عند أول تشغيل — لا تُفقد أي مسودة.
- حذف الصيغة يُبقي الأمّ؛ وحذف آخر صيغة يحذف الأمّ كاملة. الاستعادة تعيد النص الخام والمنسّق والمحاور الثلاثة.

### v2.0

- مستوى تدخل جديد: «تنسيق إيقاعي» — يوزّع الفقرات والوقفات حسب النَّفَس الداخلي للنص عبر النموذج.
- تمايز حقيقي بين المنصات الخمس تحت «تنسيق منصة»، مع منطق خاص لنمط «شذرة» عبر المنصات.
- كل ضغطة على «نسّق النص» تُنتج توزيعًا شكليًا مختلفًا (رقم محاولة + حرارة أعلى)، والمعنى والكلمات ثابتة بعقد صريح. «تنظيف فقط» يبقى محليًا حتميًا.
- زرّا «سطور أقل/أكثر» صارا يمرّان عبر النموذج بعقد «التخطيط فقط»، ويرجعان للسلوك المحلي عند تعذّر الخدمة أو مع «تنظيف فقط».
- قوائم المحاور الثلاثة تُملأ من مصدر حقيقة واحد في `main.js` يطابق ثوابت `main.rs`.

### v1.1

- إعدادات حقيقية بتبويبَين: «عام» و«المظهر».
- زر إعدادات بأيقونة فقط (منزلقات) مع تلميح ووصف لقارئات الشاشة.
- ستة ثيمات قابلة للتغيير الفوري، يُحفظ المختار محليًا ويعود عند فتح التطبيق.
- زر مستقل «المسودات» في الشريط العلوي، مع شارة بعدد المحفوظ.
- «حفظ كمسودة» يحفظ محليًا النص الأصلي والمنسّق والنمط والمستوى والمنصة والتاريخ.
- لوحة مسودات: عرض النصين، نسخ النتيجة، استعادة كاملة إلى الواجهة، وحذف بتأكيد.
- Escape يغلق اللوحات المفتوحة.

### v1.0

- النسخة الأولى: أنماط التنسيق الخمسة، مستويات التدخل الأربعة (منها «تنظيف فقط» المحلي)، تنسيق المنصات، والنسخ.

## المتطلبات

- Rust (مثبّت عبر rustup)
- Node.js (لأداة سطر أوامر Tauri فقط — الواجهة HTML/CSS/JS خام بلا اعتماديات)

## الأيقونة

المصدر `src-tauri/icons/AppIcon.icon` (Icon Composer) — «النون المشذَّبة» —
وطبقتاه `Assets/bowl.svg` و`Assets/dot.svg`.

- **البناء:** `tools/icon/build-app-icon.sh` يشغّل `xcrun actool` فينتج
  `Assets.car` و`icon.icns` و`icon.png` في `src-tauri/icons`.
- **الربط:** `Info.plist` يضبط `CFBundleIconName`، و`bundle.macOS.files`
  في `tauri.conf.json` يضيف `Resources/Assets.car`. يرسم النظام من
  `Assets.car` الوجه الموافق لمظهره: فاتحًا أو داكنًا.
- لا تبديل لأيقونة الدوك وقت التشغيل؛ أُزيل في المرحلة ١ من إعادة بناء
  الواجهة (`a524028`).
