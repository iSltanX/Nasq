# حالة NSQ

> نقطة الدخول السريعة. الخريطة الكاملة في `docs/PHASES.md`، والتفاصيل في `.local/phases/` (محلية).

**المرحلة الحالية:** ٧-ب — ربط شريط القوائم وقوائم السياق · لم تبدأ
**آخر مرحلة مكتملة:** ٦ — أول تشغيل والتحديثات · 2026-09-18
**آخر إيداع:** `PHASE6` (المرحلة ٦) · مدفوع إلى `origin/feature/v27-ui`
**الفرع:** `feature/v27-ui` · النسخة `9.0.0` · الإطلاق مستهدف عند `27.0`
**لوحة المتابعة:** https://claude.ai/artifact/2ZDgf6Hhx6ffs7uEBVwPuu — تُحدَّث على الرابط نفسه في نهاية كل مرحلة.

## آخر ما تم
اكتملت **المرحلة ٦**: ورقة أول تشغيل بخطوتين (ترحيب ← ربط النموذج) تفتح في وسط النافذة، و«لاحقًا» يُبقي التطبيق عاملًا، و«نسّق» بلا مزوّد يعيد **الخطوة الثانية وحدها** كما نصّ التصميم. وتنبيه تحديث بحالاته الثلاث المرسومة.

**والتصميم صحّح الخطة:** التنبيهات **ثلاثة لا أربعة**. لا تنبيه «جاهز للتثبيت» لأن زر «ثبّت وأعد التشغيل» موافقةٌ واحدة تغطّي التنزيل والتثبيت وإعادة التشغيل — وهو ما يقوله تدفّق ٧ أيضًا (٤ خطوات، ٣ حالات).

**وفجوتا المرحلة ٥ أُغلقتا:** للتنزيل والتثبيت واجهتهما الآن (وقدرتهما في النافذة الرئيسية وحدها)، و`autoUpdates` صار له قارئ: فحصٌ تلقائي عند الإقلاع **صامت** — لا يعرض «لا تحديث» ولا خطأً إلا لمن طلب الفحص بنفسه.

**والمشترك صار مصدرًا واحدًا:** ورقة الربط تستعمل مكوّن «Form Section» ومنتقيه نفسهما اللذين في نافذة الإعدادات، فاستُخرجا إلى `forms.css` و`forms.js` و`providers.js` بدل نسخهما. و`load_settings` يُنادى مرة واحدة يتقاسمها الثلاثة عبر `NasaqShell.settingsReady`.

## الخطوة التالية
ابدأ **٧-ب** (ربط الواجهة وقوائم السياق): بقيّة عناصر الشريط بمسرّعاتها وحذف مستمعات `keydown` التي صار لها مسرّع، وفتحها بـ`set_menu_state`، وقوائم السياق عبر حافظة Rust. وفيها تُحسم فجوتا لوحة القوائم أدناه.

## آخر تحقق ناجح
- `npm test` — **142/142** ناجحة، منها `isolation.test.js` 57/57 و`foundations.test.js` 7/7.
- `cargo test` داخل `src-tauri` — **125 ناجحة**، و`cargo clippy --all-targets` بالتحذيرات الثمانية نفسها التي كانت قبل المرحلة (تحقّقت بـ`git stash`).
- قياس في المتصفح طابق إطارات التصميم رقمًا رقمًا: ورقة الترحيب ٤٦٤٫٥ وورقة الربط ٣٨٤، وصفّ الميزة ٤٠، وصفوف النموذج ٣٨٫٥ و٤٠، والتنبيهات ٢٣٢ و٢٢٢ و٢١٢ بأجسامها ١٤٨ و١٣٨ و١٢٨. (فرق ٢ في كل ورقة وتنبيه هو حدّا الشعرة اللذان يحسبهما Figma ولا يحسبهما CSS — الاصطلاح نفسه الذي أثبتته المرحلة ٥.)
- فحص سلوكي بمحاكاة النواة: أول تشغيل، وتبديل المزوّد، وحفظ المفتاح وتقنيعه، واختبار الاتصال، وOllama بلا صفّ مفتاح، و«لاحقًا» و«ابدأ»، و«نسّق» بلا مزوّد، وEsc بمستوياته، والتنبيهات الثلاثة، وسلسلة تنزيل ← تثبيت ← إعادة تشغيل، و«إلغاء» أثناء التنزيل، والفحص التلقائي الصامت.
- انحدار نافذة الإعدادات بعد استخراج المشترك: أقلعت، والمنتقي والتبويبان وEsc تعمل كما كانت.
- **١٢ حارسًا جديدًا، كُسِر كل واحد عمدًا في نسخة مؤقتة وفشل قبل اعتماده** (١٢ كسرًا، ١٢ فشلًا).

## القرارات الحاكمة
1. التصميم يقود والكود يتبعه. ما في الكود ولا وجود له في Figma يُصمَّم في Figma أولًا.
2. العزل بين نَسَق وشَذْب فوق كل شيء: لا دمج برومبتات ولا عقود ولا منطق مخرجات، ولا استيراد متبادل، ولا نقل ثابت بينهما ولو تطابق حرفيًا.
3. كل مرحلة تحذف ما استبدلته من أجزاء مؤقتة؛ لا مرحلة تنظيف مؤجَّلة.
4. الفحص البصري تمريرة واحدة في المرحلة ٨، لا إصلاحات بصرية متفرقة قبلها.
5. لا تشغيل للتطبيق على جهاز المالك بلا إذن؛ التحقق بالمتصفح والاختبارات.
6. الدفع على فرع العمل بعد كل إنجاز متحقَّق. لا دفع إلى `main` ولا وسم ولا فتح المستودع قبل v27.0 — وذلك قرار المالك.
7. إطارات Figma تتقدّم على أوصاف المكوّنات النصية عند تعارضهما؛ الوصف قد يتخلّف عن آخر تمريرة تصميم.
8. **ما كان على مستوى التطبيق لا على مستوى برج** (ورقة الترحيب، تنبيه التحديث) بلا `data-for`، ولونه لون الهوية ثابتًا (`accent-nasaq`) لا لون الوحدة الظاهرة.

## المشاكل والمعلّقات
- **لم يبقَ جزء مؤقت في الواجهة.**
- **خطّاف `NSQ_ENTRY`** في `src-tauri/src/app/window.rs:74` لنسخ التطوير — يُحذف في المرحلة ٩.
- **مؤجَّل إلى المرحلة ٨** من المرحلة ٣: الوضع الفاتح، والبحث والعدسة والتنويعات بنموذج عامل، وتنبيه الحذف، وعودة التركيز بعد الأوراق.
- **مؤجَّل إلى المرحلة ٨** من المرحلة ٤: صياغة ملخّص «النتيجة» حين تتعذّر قصّة، ورمز القصّة المتعذّرة، والوضع الفاتح والتباين العالي وتقليل الحركة، وتمريرة لوحة مفاتيح كاملة.
- **مؤجَّل إلى المرحلة ٨** من المرحلة ٦: الوضع الفاتح للورقة والتنبيه، وتمريرة لوحة مفاتيح على الورقة بخطوتيها.
- **فجوة تصميم من المرحلة ٦: حالة الفشل غير مرسومة** — لا تنبيه لتعذّر الفحص ولا لتعذّر التنزيل. بُنيت بهيئة «لا تحديث» نفسها برسالة السبب، وتحتاج إطارًا في Figma.
- **«إلغاء» أثناء التنزيل لا يقطع الطلب الجاري**، بل يمنع ما بعده: البايتات الواصلة تُهمل ولا يُثبَّت منها شيء ولا إعادة تشغيل. قطعٌ حقيقي يحتاج عقدًا في النواة، وليس في التصميم ما يطلبه.
- **`branding/sultan/`** بلا أي مرجع من الكود، ولوحة «حول» لم تستعمل المخطوطة — فمصيره اليوم قرار المالك: يبقى أو ينتقل إلى مخزن هويته خارج المستودع.
- **قائمة «المظهر» غير مرسومة** في التصميم (المرسوم قيمتها «تلقائي» فحسب)، فاستُعملت ثلاثية النظام: تلقائي/فاتح/داكن.
- **عناصر شريط القوائم التي تنفّذها الواجهة تبدأ معطّلة** عمدًا؛ تفتحها الواجهة بـ`set_menu_state` حين تجهز. فُتح منها «التحقق من وجود تحديثات…» في المرحلة ٦، والبقية من شغل ٧-ب.
- **فجوتان في لوحة القوائم تنتظران قرار المالك:** عناصر قائمة «بحث ▸» غير مرسومة (وُضع فيها مؤقتًا ما بنته المرحلة ٣: ⌘F و⌘G و⇧⌘G)، وأوامر شَذْب بلا مكان في الشريط رغم قاعدة اللوحة «كل أمر له مكان واختصار ثابت».
- **تكرار مقصود مؤقتًا:** خوارزمية «أوجد الظهور رقم n» في `locate` بـ`src/shadhb.js` تكرّر الحلقة داخل `applyCuts` في `src/prune.js`. حلّها تصدير دالة بحث من المقص واستهلاكها من الطرفين — وهو مساسٌ بالمقص منعته المرحلة ٤، فيُعالَج حين يُفتح لتعديل مع اختباراته.

## ملفات الخطوة التالية
`src-tauri/src/app/menu.rs` · `src/layout.js` (واجهة القوائم) · `src/shell.js` · `src/nasaq.js` و`src/shadhb.js` (مستمعات `keydown` التي صار لها مسرّع) · `src-tauri/src/shared/clipboard.rs` · `tests/isolation.test.js`
