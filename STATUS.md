# حالة NSQ

> نقطة الدخول السريعة. الخريطة الكاملة في `docs/PHASES.md`، والتفاصيل في `.local/phases/` (محلية).

**المرحلة الحالية:** ٩ — إصدار v27.0 · جارية: ٩-أ…٩-ح مُودَعة، وجولة الفحص m1–m8 مغلقة (`AUDIT-STATUS.md`)؛ الباقي البناء الموقّع والوسم والإصدار
**آخر مرحلة مكتملة:** ٨ — تمريرة الجودة · 2026-09-18
**آخر إيداع برمجي:** `5c10373` (فحص m7-12) · وبعده وثائق وفحص ما قبل النشر العام
**الفرع:** `feature/v27-ui`، ويُدمج في `main` · النسخة `27.0.0`
**لوحة المتابعة:** https://claude.ai/artifact/2ZDgf6Hhx6ffs7uEBVwPuu — تُحدَّث على الرابط نفسه في نهاية كل مرحلة.

## آخر ما تم

**2026-09-24 — فحص ما قبل النشر العام** بأمر المالك: لا سرّ في الشجرة ولا في التاريخ
(gitleaks على كل الإيداعات، وأنماط مفاتيح على كل نسخة ملفٍّ مرّت بالمستودع)، وتصحيح README
وCHANGELOG لإصدار 27.0.0، ولقطات حقيقية من نسخة إصدار معزولة البيانات، ثم دمج `main`
وفتح المستودع للعامة.

**قبلها — المرحلة ٨:** اكتملت، ومعيارها يطلب «كل إطار بجانب لقطته في أربعة أوضاع وعند
عرضين» — أي ١٦٠ لقطة، تفحُّصها بالعين يضمن التعب لا التغطية. فقُدْتُ التطبيق
بمتصفّح حقيقي وفحصتُ **حسابيًا**: تباين WCAG لكل عقدة نص، والفيضان عند ٧٦٠،
ومُدد الحركة. ثم صوّرتُ ما يحتاج حكمًا بشريًا وحده.

**صفر خرق تباين وصفر فيضان** في ٥ أوضاع × عرضين، وفي نحو ٢٥ تركيبة حالة×مظهر.
و**النوافذ الثلاث 100/100** في فحص Lighthouse للوصول (كانت الرئيسية ٩٤ و«حول» ٩٥).

**وأهمّ ما في المرحلة درسٌ في حدود الفحص الآلي:** «التقرير» في المفتّش كان
**نصًّا في DOM بلا ارتفاع على الشاشة** — «ما تغيّر» و«بصمة الإيقاع» عنوانان بلا
شيء تحتهما. لم يره فاحصُ التباين، لأن عنصرًا بارتفاعٍ صفر ليس ضعيف التباين بل
غير مرسوم أصلًا فيتخطّاه كل فاحص. رأته **المقابلة بالإطار `100:545`** وحدها.

والسبب انحدارٌ من المرحلة ٦: استُخرجت `.row-value` إلى `forms.css` وهي لصفٍّ
**أفقي**، وتحمّلها النافذة الرئيسية أيضًا — فطوى الأساسُ صفرًا ارتفاعَ «التقرير»
وهو صفٌّ **عمودي**، وابتلع القصُّ نصَّه.

**وأربعة عيوب في لوحة Figma نفسها** كشفها الجرد قبل أي رسم: «عرض» بعشرة عناصر
كلها مؤشَّرة وفي العقد اثنان، و«بحث» و«الخدمات» — رأسا قائمتين فرعيتين — تعرضان
اختصارًا، وقائمة المزوّد تؤشّر الستة كلها.

## آخر تحقق ناجح
- **2026-09-24:** `npm test` — **399/399** (العزل أخضر)، و`cargo test` — **176/176**، وclippy ٤/٩ تحذيرات بلا خطأ، وبناء إصدارٍ ناجح.
- **المرحلة ٨:** `npm test` — **153/153**، و`cargo test` — **126/126**، وclippy بتحذيراته الثمانية نفسها.
- **Lighthouse:** `index.html` و`settings.html` و`about.html` — **100/100** وصولًا.
- **تشغيل حقيقي بإذن المالك** على نسخة إصدار مبنيّة: الإقلاع **١٢١٠ م.ث باردًا**
  و**٣١٦–٣٢٧ م.ث دافئًا** إلى أول نافذة مرسومة (خمس تشغيلات). الحزمة **٩٫٩ م.ب**.
  قُرئت مسودات المالك الاثنتا عشرة ولم تُمسّ.
- **خمسة حرّاس جديدة، كُسِر كلٌّ عمدًا وفشل قبل اعتماده** — وبعضها بأربع صيغ،
  منها «ردٌّ ناقص» و«قيمة خاطئة» لا الحذف وحده.

## القرارات الحاكمة
1. التصميم يقود والكود يتبعه. ما في الكود ولا وجود له في Figma يُصمَّم في Figma أولًا.
2. العزل بين نَسَق وشَذْب فوق كل شيء: لا دمج برومبتات ولا عقود ولا منطق مخرجات، ولا استيراد متبادل، ولا نقل ثابت بينهما ولو تطابق حرفيًا.
3. كل مرحلة تحذف ما استبدلته من أجزاء مؤقتة؛ لا مرحلة تنظيف مؤجَّلة.
4. الفحص البصري تمريرة واحدة في المرحلة ٨ — **تمّت**.
5. لا تشغيل للتطبيق على جهاز المالك بلا إذن. (أُذن مرة في ٨-هـ لقياس الإقلاع.)
6. الدفع على فرع العمل بعد كل إنجاز متحقَّق. **دمج `main` وفتح المستودع للعامة بأمر المالك (2026-09-24)** بعد فحص ما قبل النشر؛ والوسم والإصدار بقراره.
7. إطارات Figma تتقدّم على أوصاف المكوّنات النصية عند تعارضهما.
8. **ما كان على مستوى التطبيق لا على مستوى برج** (ورقة الترحيب، تنبيه التحديث، لوحة «حول») بلا `data-for`، ولونه لون الهوية ثابتًا (`accent-nasaq`).
9. **ملفٌّ تحمّله نافذتان يسري عليهما معًا ولو كُتب لإحداهما** — درس `.row-value`. والعمود الذي يعيد استعمال صنف صفٍّ يردّ `flex` و`overflow` و`white-space`.
10. **الإذن بقدر الحاجة بالاسم:** لا نافذة تملك فتح رابطٍ لم يُذكر في قدرتها، ولا إذن زائد لا تستعمله.

## المشاكل والمعلّقات
- **لم تبقَ ملاحظة بصرية مؤجَّلة من المراحل ٣–٧.** ولا فجوة تصميم معلومة.
- **VoiceOver لم يُشغَّل بنفسه.** فُحص **ما يقرؤه**: الأدوار والتسميات والمناطق
  الحيّة وترتيب العناوين والمعالم، آليًا وبـLighthouse. أما جلسة استماع حقيقية
  فتحتاج قارئ الشاشة على جهاز المالك، ولم تُطلب.
- **«إلغاء» أثناء التنزيل لا يقطع الطلب الجاري**، بل يمنع ما بعده: البايتات
  الواصلة تُهمل ولا يُثبَّت منها شيء. قطعٌ حقيقي يحتاج عقدًا في النواة، وليس في
  التصميم ما يطلبه.
- **تكرار مقصود مؤقتًا:** خوارزمية «أوجد الظهور رقم n» في `locate` بـ`src/shadhb.js` تكرّر الحلقة داخل `applyCuts` في `src/prune.js`. حلّها تصدير دالة بحث من المقص واستهلاكها من الطرفين — وهو مساسٌ بالمقص منعته المرحلة ٤، فيُعالَج حين يُفتح لتعديل مع اختباراته.

## ملفات الخطوة التالية
المرحلة ٩ (الإصدار): النسخة `27.0.0` وحذف `NSQ_ENTRY` (`1cb2fa2`)، وREADME وCHANGELOG بلقطات
حقيقية — تمّت. الباقي: بناءٌ موقّع بمفتاح التوقيع (`TAURI_SIGNING_PRIVATE_KEY`، خارج المستودع)
يُنتج `latest.json`، ثم الوسم `v27.0.0` وإصداره على GitHub؛ فتصل إليه نسخُ 9.0.0 بالمحدِّث لأن
نقطة التحديث ومفتاحها العام لم يتغيّرا.
