# حالة NSQ

> نقطة الدخول السريعة. الخريطة الكاملة في `docs/PHASES.md`، والتفاصيل في `.local/phases/` (محلية).

**المرحلة الحالية:** ٥-ب — صفحة الإعدادات ونافذة «حول» · لم تبدأ
**آخر مرحلة مكتملة:** ٤ — شَذْب كاملًا · `c383a84` · 2026-09-17
**آخر إيداع:** `1727c34` (دمج الشقّ الخلفي) · مدفوع إلى `origin/feature/v27-ui`
**الفرع:** `feature/v27-ui` · النسخة `9.0.0` · الإطلاق مستهدف عند `27.0`
**لوحة المتابعة:** https://claude.ai/artifact/2ZDgf6Hhx6ffs7uEBVwPuu — تُحدَّث على الرابط نفسه في نهاية كل مرحلة.

## آخر ما تم
بُني برج **شَذْب** كاملًا من صفحة Figma `51:9` (`c383a84`)، ثم دُمج **الشقّ الخلفي** الذي جرى بالتوازي على `feature/v27-ui-core` (`1727c34`، تقديم سريع بلا تعارض — صفر تقاطع في الملفات):
- **٥-أ** (`7cfd01c`): خزنة المفتاح في Keychain مع ترحيل لا يمسّ `settings.json` إلا بعد قراءة تحقّق، و`test_connection` لكل مزوّد عبر نقطة سرد النماذج يفرّق بين مفتاح مرفوض واسم نموذج خاطئ، وخدمة نافذة الإعدادات (عرض ٥٠٠ ثابت وارتفاع يتبع اللوح).
- **٧-أ** (`8a532cd`): شريط القوائم الأصلي من لوحة `265:1299` مواصفةً بيانات خالصة، والفعل الرئيس ⌘↩ عنصر واحد تملك القشرة اسمَيه.
- **وقطعتان** (`d00933d`): اللصق من النواة عبر `pbpaste` (قوائم السياق تحتاجه بلا مطالبة إذن)، ونافذة «حول» بمقاس لوحة الماك المقيس ٢٨٤×٢١٢.

## الخطوة التالية
ابدأ **٥-ب**: `src/settings.html` و`src/about.html` وربطهما بالأوامر الجاهزة، وحذف الطبقة المؤقتة. الأساس الخلفي كله جاهز ومدموج — الأوامر المتاحة: `open_settings`، `open_about`، `secondary_window_ready`، `settings_pane_resized`، `test_connection`، `set_menu_state`، `set_active_module`، `set_active_pane`، `read_from_clipboard`.

اقرأ `docs/PHASES.md`، ثم `.local/phases/05-settings/` (فيه `PLAN.md` و`REPORT-core.md`)، ثم صفحة «نوافذ التطبيق» `250:80` في Figma (الإعدادات `250:81` و«حول» `254:502`)، ثم طبقة الإعدادات المؤقتة في `src/index.html:551` و`src/app.css:1919` و`src/shell.js:245`.

## آخر تحقق ناجح
- `npm test` — 123/123 ناجحة، منها `isolation.test.js` 39/39 و`foundations.test.js` 6/6.
- `cargo test` داخل `src-tauri` — **120 ناجحة** (كانت 69 قبل الشقّ الخلفي)، و`cargo clippy` بلا ملاحظة على الوحدات الجديدة.
- البناء بعد الدمج ناجح، واختبارات الطرفين خضراء معًا.
- كل حارس جديد كُسِر عمدًا في نسخة مؤقتة وفشل قبل اعتماده (13 كسرًا، 13 فشلًا).
- تحقّق سلوكي في معاينة المتصفح على الحالات السبع، عند 1280 و760، فاتح وداكن: القيم طابقت إطارات Figma حرفيًا.

## القرارات الحاكمة
1. التصميم يقود والكود يتبعه. ما في الكود ولا وجود له في Figma يُصمَّم في Figma أولًا.
2. العزل بين نَسَق وشَذْب فوق كل شيء: لا دمج برومبتات ولا عقود ولا منطق مخرجات، ولا استيراد متبادل، ولا نقل ثابت بينهما ولو تطابق حرفيًا.
3. كل مرحلة تحذف ما استبدلته من أجزاء مؤقتة؛ لا مرحلة تنظيف مؤجَّلة.
4. الفحص البصري تمريرة واحدة في المرحلة ٨، لا إصلاحات بصرية متفرقة قبلها.
5. لا تشغيل للتطبيق على جهاز المالك بلا إذن؛ التحقق بالمتصفح والاختبارات.
6. الدفع على فرع العمل بعد كل إنجاز متحقَّق. لا دفع إلى `main` ولا وسم ولا فتح المستودع قبل v27.0 — وذلك قرار المالك.
7. إطارات Figma تتقدّم على أوصاف المكوّنات النصية عند تعارضهما؛ الوصف قد يتخلّف عن آخر تمريرة تصميم.

## المشاكل والمعلّقات
- **أجزاء مؤقتة في الكود، معلَّمة بتعليقاتها:** طبقة الإعدادات داخل الصفحة (`src/index.html` و`src/app.css` و`src/shell.js`) تُستبدل في المرحلة ٥. لم يبقَ جزء مؤقت لشَذْب.
- **خطّاف `NSQ_ENTRY`** في `src-tauri/src/app/window.rs:74` لنسخ التطوير — يُحذف في المرحلة ٩.
- **مؤجَّل إلى المرحلة ٨** من المرحلة ٣: الوضع الفاتح، والبحث والعدسة والتنويعات بنموذج عامل، وتنبيه الحذف، وعودة التركيز بعد الأوراق.
- **مؤجَّل إلى المرحلة ٨** من المرحلة ٤: صياغة ملخّص «النتيجة» حين تتعذّر قصّة (لا صفّ يسمّيها)، ورمز القصّة المتعذّرة (لا مثلث تحذير في مجموعة الرموز)، والوضع الفاتح والتباين العالي وتقليل الحركة، وتمريرة لوحة مفاتيح كاملة.
- **`branding/sultan/`** بلا أي مرجع من الكود؛ يُحسم مصيره في ٥-ب حسب ما تستعمله نافذة «حول».
- **نافذتا الإعدادات و«حول» لا تفتحان بعد:** خدمتاهما في النواة جاهزتان، وصفحتاهما (`settings.html` و`about.html`) من شغل ٥-ب.
- **عناصر شريط القوائم التي تنفّذها الواجهة تبدأ معطّلة** عمدًا (حدث القائمة لا يُخزَّن لمستمع يلتحق متأخرًا)؛ تفتحها الواجهة بـ`set_menu_state` حين تجهز — من شغل ٧-ب.
- **فجوتان في لوحة القوائم تنتظران قرار المالك:** عناصر قائمة «بحث ▸» غير مرسومة (وُضع فيها مؤقتًا ما بنته المرحلة ٣: ⌘F و⌘G و⇧⌘G)، وأوامر شَذْب بلا مكان في الشريط رغم قاعدة اللوحة «كل أمر له مكان واختصار ثابت».
- **تكرار مقصود مؤقتًا:** خوارزمية «أوجد الظهور رقم n» في `locate` بـ`src/shadhb.js` تكرّر الحلقة داخل `applyCuts` في `src/prune.js`. حلّها تصدير دالة بحث من المقص واستهلاكها من الطرفين — وهو مساسٌ بالمقص منعته المرحلة ٤، فيُعالَج حين يُفتح لتعديل مع اختباراته.

## ملفات الخطوة التالية
`src/index.html` (طبقة `#settings-overlay`) · `src/app.css` (`.interim-overlay`) · `src/shell.js:245` وما بعده · `src-tauri/src/app/` · `src-tauri/src/shared/settings.rs` · `tests/isolation.test.js`
