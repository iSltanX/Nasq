# حالة NSQ

> نقطة الدخول السريعة. الخريطة الكاملة في `docs/PHASES.md`، والتفاصيل في `.local/phases/` (محلية).

**المرحلة الحالية:** ٤ — شَذْب كاملًا · لم تبدأ
**آخر مرحلة مكتملة:** ٣ — نَسَق كاملًا · `0cae9b1` · 2026-09-17
**الفرع:** `feature/v27-ui` · النسخة `9.0.0` · الإطلاق مستهدف عند `27.0`

## آخر ما تم
تأسيس نظام تتبّع المراحل داخل المستودع (`STATUS.md`, `docs/PHASES.md`, `.local/phases/`)، وكنس الملفات الميتة ومخلّفات الجيل السابق من الواجهة، وإعادة كتابة `README.md` على الحالة الفعلية.

## الخطوة التالية
ابدأ المرحلة ٤ (شَذْب). اقرأ `docs/PHASES.md` ثم `.local/phases/04-shadhb/PLAN.md`، ثم صفحة شَذْب `51:9` في Figma بأقسامها، ثم الملفات المرتبطة.

## آخر تحقق ناجح
- `npm test` — 114/114 ناجحة، منها `isolation.test.js` 30/30.
- `cargo test` داخل `src-tauri` — 69 ناجحة.

## القرارات الحاكمة
1. التصميم يقود والكود يتبعه. ما في الكود ولا وجود له في Figma يُصمَّم في Figma أولًا.
2. العزل بين نَسَق وشَذْب فوق كل شيء: لا دمج برومبتات ولا عقود ولا منطق مخرجات، ولا استيراد متبادل، ولا نقل ثابت بينهما ولو تطابق حرفيًا.
3. كل مرحلة تحذف ما استبدلته من أجزاء مؤقتة؛ لا مرحلة تنظيف مؤجَّلة.
4. الفحص البصري تمريرة واحدة في المرحلة ٨، لا إصلاحات بصرية متفرقة قبلها.
5. لا تشغيل للتطبيق على جهاز المالك بلا إذن؛ التحقق بالمتصفح والاختبارات.
6. الدفع على فرع العمل بعد كل إنجاز متحقَّق. لا دفع إلى `main` ولا وسم ولا فتح المستودع قبل v27.0 — وذلك قرار المالك.

## المشاكل والمعلّقات
- **أجزاء مؤقتة في الكود، معلَّمة بتعليقاتها:** قصّات شَذْب وبطاقة القراءة (`src/app.css:1451`, `src/index.html:318`) تُستبدل في المرحلة ٤؛ وطبقة الإعدادات داخل الصفحة (`src/index.html:551`, `src/app.css:1919`, `src/shell.js:245`) تُستبدل في المرحلة ٥.
- **مفتاح إطفاء شَذْب** `localStorage["nasaq-shadhb"]` يَعِد بالعودة إلى واجهة سابقة (`src/shadhb.js:8`) — يُحسم في المرحلة ٤.
- **خطّاف `NSQ_ENTRY`** في `src-tauri/src/app/window.rs:74` لنسخ التطوير — يُحذف في المرحلة ٩.
- **مؤجَّل إلى المرحلة ٨** من المرحلة ٣: الوضع الفاتح، والبحث والعدسة والتنويعات بنموذج عامل، وتنبيه الحذف، وعودة التركيز بعد الأوراق.
- **`branding/sultan/`** بلا أي مرجع من الكود؛ يُحسم مصيره في المرحلة ٥ حسب ما تستعمله نافذة «حول».

## ملفات الخطوة التالية
`src/shadhb.js` · `src/index.html` (مناطق `data-for="shadhb"`) · `src/app.css:1451-1560` · `src/prune.js` · `src-tauri/src/shadhb/` · `tests/isolation.test.js`
