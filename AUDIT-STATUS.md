# حالة فحص NSQ

> نقطة الدخول لمسار الفحص: **«ابدأ mN» يبدأ المرحلة N** (m1 … m8). الخريطة والقواعد والمصادر والبروتوكول في `docs/AUDIT-PHASES.md`، والتفاصيل في `.local/audit/` (محلية).
> يجري الفحص على الفرع `audit/v27` في شجرة عمل مستقلة: `/Volumes/iSltanXx/Projects/NSQ-audit`.
> **الفحص مستقلّ عن مراحل البناء:** لا يقرأ وثائقها ولا يأخذ منها شيئًا غير رابط Figma ومعرّفات صفحاته (القاعدة ١٠).

**المرحلة الحالية:** m3 — الصلابة والأداء · لم تبدأ
**آخر مرحلة مكتملة:** m2 — سلامة البيانات والحفظ · 2026-09-18
**آخر إيداع للفحص:** `720c926` و`82a5dc6` (إصلاحات m2)، ثم وثائقها
**الفرع:** `audit/v27`، مدفوع. وفيه أيضًا `0efb613` و`db5e154` (أشرطة التمرير) من جلسة المالك الأخرى `nsq-audit-2a`، وليسا من m2
**سجلّ الملاحظات:** `.local/audit/LEDGER.md` — ٣٠ صفًّا (٢٠ للتطبيق و١٠ لأداة القياس)، و١٠ بنود مؤجَّلة إلى m7
**لوحة الفحص:** https://claude.ai/artifact/N4RmWpMSpqhNN83jcg5WP6 — مستقلة، وفي لوحة البناء رابطٌ إليها
**دمجٌ حرِج مُرجأ:** إصلاحات m2 تمنع ضياع بيانات، فحقّها الدمج في `feature/v27-ui` في نهاية المرحلة (القاعدة ٩). لكن شجرة المجلد الرئيسي غير نظيفة (`.claude/launch.json` معدَّل)، فأُرجئ الدمج. ويجري حين تنظف شجرته، أو مع الدمج بعد m8

## آخر ما تم

**2026-09-18 — اكتملت m2.** كل حكمٍ من اختبارٍ فشل على الكود القديم، أو من مشهدٍ قيس قبل الإصلاح وبعده.

- **ضياع بيانات، أُصلح:**
  - m1-01 تأكّد: ملفٌّ بُتر وسط حرف، أو ليس قائمة، يكتب أول حفظٍ فوقه بلا نسخة.
  - m2-01: الكتابة غير ذرّية. وجد قارئٌ الملف ناقصًا ١١٥–١٣٤ مرة أثناء الحفظ.
  - m2-02: تنحيةٌ ثانية تمحو الأولى.

  الإصلاح وحدة `shared/storage.rs`: كتابةٌ ذرّية 0600، وتنحيةٌ بربطٍ صلب لا يمحو. ومعها رفضُ الحفظ بعد قراءةٍ فاشلة.
- **أعطالٌ يراها الكاتب، أُصلحت:**
  - الإعدادات التالفة لا يمرّ حفظها.
  - من أضاف مفتاحه في نافذة الإعدادات يُمنع من التنسيق.
  - حقل النموذج يمتلئ بالافتراضي تحت المؤشر، فحُفظ «claude-opus-5gpt-x».
  - نتيجة «تحقّق الآن» تُمحى قبل أن تُرى.
  - ومعها أربعة متوسطة: رسالة الاستيراد، وتراجع حذف الصيغة، وصيغةٌ أحدث، وملفٌّ آخر يُستورد.
- **العزل:** حارس «لا عقد في الأساس» كان بقائمةٍ مكتوبة باليد لا تشمل `secrets.rs`. صار يقرأ المجلدات.
- **سبعة أسئلة للمالك:** في «المشاكل والمعلّقات» أدناه.

## آخر تحقق ناجح

m2، 2026-09-18، على `82a5dc6`:
- `npm test` **١٦٨/١٦٨**، و`isolation.test.js` **٧٦/٧٦**، و`cargo test` **١٤٠/١٤٠**.
- clippy: **٤** للبرنامج و**٩** مع الاختبارات، كما في m1، ولا تحذير في كود m2.
- انحدار المشغّلات السبعة على كود الإصلاح: صفر خرق حقيقي، وصفر فيضان، والحركة ٣٠٠ و١٥٠. وحالة القوائم تطابق الأزرار في ست خطوات.
- مراجعة مستقلة (`code-reviewer`، Sonnet): لا حرِج ولا عالٍ. أُخذ بملاحظتين (`82a5dc6`).
- لم يُقس بعد: الإقلاع والذاكرة (m7)، وحجم الحزمة (m8)، وزمن `sync_all` على الخيط الرئيس (m3).

## القرارات الحاكمة

القواعد كاملة بأسبابها في `docs/AUDIT-PHASES.md`. أهمّها:
1. مرحلة واحدة ثم توقّف حتى «ابدأ mN».
2. الدليل قبل الحكم: `ملف:سطر` أو مخرج أمر، بصفة **مؤكَّد** أو **من القراءة** أو **[غير محدد - بانتظار التفاصيل]**.
3. القياس قبل الإصلاح، والإصلاح في نطاق المرحلة بحارسٍ كُسِر عمدًا.
4. التصميم يقود: ما يغيّر الشكل أو يضيف سلوكًا يمرّ بـFigma وبقرار المالك.
5. العزل بين نَسَق وشَذْب فوق كل شيء، و`isolation.test.js` أخضر؛ وما يمسّ برومبتًا أو عقدًا يوقف العمل.
6. لا تشغيل للتطبيق إلا في m7، وبإذنٍ في حينه.
7. الوكلاء Sonnet 5 فقط، والحكم في الهوية والتصميم للجلسة الرئيسية.
8. الإيداع على `audit/v27` بمسارات صريحة ثم دفعه. والدمج في `feature/v27-ui` مرةً بعد m8، والحرِج في نهاية مرحلته، من المجلد الرئيسي وشجرته نظيفة. ولا إصدار علني قبله، ولا `main` ولا وسم من الفحص.
9. لا رقمٌ من المتصفح بغير `fresh: true` و`requestFailed: []`.
10. **الاستقلال:** لا قراءة لوثائق مسار البناء ولا لأدواته ولا لذاكرة مراحله، ولا أخذ منها. المصادر هي الكود، وما يقيسه الفحص، وFigma: `DZQACOUgD70llKedxWcHbK`، والصفحات `51:3` و`51:5` و`51:6`.

## المشاكل والمعلّقات

- **الدمج الحرِج لإصلاحات m2 مُرجأ:** شجرة المجلد الرئيسي غير نظيفة (`.claude/launch.json` معدَّل). ويجري حين تنظف، أو مع دمج ما بعد m8.
- **أسئلة المالك من m2** (قرار منتج، لم يُنفَّذ منها شيء، والتفصيل في `m2-data/REPORT.md`):
  1. حفظ النص الجاري واسترجاع الجلسة (m2-16).
  2. تنبيهٌ قبل الإغلاق بصيغٍ غير محفوظة (m2-16).
  3. تأكيدٌ قبل «استعادة إلى المحرر» فوق عملٍ غير محفوظ (m2-14)، وFigma أولًا.
  4. سقف المئة مسودة يحذف الأقدم بصمت (m2-13).
  5. صيغ النص السابق تسقط حين تتغيّر كلماته (m2-15).
  6. إعلام الكاتب حين يُنحّى ملفٌّ تالف، أو يتعذّر تحميل الإعدادات (m2-17).
  7. قفل النسخة الواحدة إن أثبت m7 أن نسختين تعملان معًا (m2-22).
- **ومن قبل:** التراجع عن «احذف القصّة» (m4)، وإسقاط ملف نصّي في المحرر (m4 وm7).
- **جلستان على شجرة الفحص:** جلسة المالك الأخرى تستعمل المرآة 4186 ومدخلها في `.claude/launch.json`. فالمدخل باقٍ لها غير مودَع، وقياس الفحص على منفذٍ منفصل حين تعمل.

## ملفات الخطوة التالية

- **m3:** `.local/audit/m3-robustness/PLAN.md` (أُضيف إليه ما سلّمه m2 عن `sync_all` وسياق `Blocking`)، و`LEDGER.md`، و`m2-data/REPORT.md` وحده.
- **الكود:** `src-tauri/src/shared/llm.rs` وأوامر البرجين، و`src/lines.js` و`prune.js` و`fragments.js`، و`src/updates.js`، و`src-tauri/src/shared/storage.rs`.
- **أداة القياس:** `.playwright-mcp/nsq-runners/README.md`؛ و`build.py` يقبل `NSQ_AUDIT_PORT` و`NSQ_AUDIT_OUT` حين يكون 4186 مشغولًا.
