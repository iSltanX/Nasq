# حالة فحص NSQ

> نقطة الدخول لمسار الفحص: **«ابدأ mN» يبدأ المرحلة N** (m1 … m8). الخريطة والقواعد والمصادر والبروتوكول في `docs/AUDIT-PHASES.md`، والتفاصيل في `.local/audit/` (محلية).
> يجري الفحص على الفرع `audit/v27` في شجرة عمل مستقلة: `/Volumes/iSltanXx/Projects/NSQ-audit`.
> **الفحص مستقلّ عن مراحل البناء:** لا يقرأ وثائقها ولا يأخذ منها شيئًا غير رابط Figma ومعرّفات صفحاته (القاعدة ١٠).

**المرحلة الحالية:** m4 — السلوك التفاعلي ولوحة المفاتيح · لم تبدأ
**آخر مرحلة مكتملة:** m3 — الصلابة والأداء · 2026-09-18
**آخر إيداع للفحص:** `6c15923` (إصلاحات m3)، ثم وثائقها
**الفرع:** `audit/v27`، مدفوع
**سجلّ الملاحظات:** `.local/audit/LEDGER.md` — ٤٣ صفًّا (٣٢ للتطبيق و١١ لأداة القياس)، و١٤ بندًا مؤجَّلًا إلى m7
**لوحة الفحص:** https://claude.ai/artifact/N4RmWpMSpqhNN83jcg5WP6 — مستقلة، وفي لوحة البناء رابطٌ إليها
**دمجٌ حرِج مُرجأ:** إصلاحات m2 تمنع ضياع بيانات، فحقّها الدمج في `feature/v27-ui` (القاعدة ٩)، لكن شجرة المجلد الرئيسي غير نظيفة (`.claude/launch.json` معدَّل). يجري حين تنظف، أو مع الدمج بعد m8. ولم يُصلح m3 عيبًا حرِجًا

## آخر ما تم

**2026-09-18 — اكتملت m3.** كل حكمٍ من اختبارٍ فشل على الكود القديم، أو من مشهدٍ قيس قبل الإصلاح وبعده.

- **أُصلح (عالٍ):** «إلغاء» أثناء تثبيت التحديث كان يُغلق التنبيه ثم يعيد تشغيل التطبيق، فيضيع ما لم يُحفظ (m3-01).
- **أُصلح (متوسط):**
  - «ابحث عن تحديثات» كان يُبتلع أثناء فحصٍ أو تنزيلٍ مُلغى (m3-02).
  - «فصل الجمل» كان يترك «»» أو «)» يتيمة أول السطر، و«!» وحدها بعد «حقًّا؟»! (m3-03، ومن المراجعة المستقلة).
  - رسائل المزوّد: ناتج Ollama المقطوع كان يُقبل، ورفض OpenAI كان «نتيجة فارغة»، وأعطالٌ ليست في الإعدادات كانت «تحقق من الإعدادات» (m3-04 – m3-06).
- **قيس ولا عيب:**
  - الأوامر المتزامنة تجري على الخيط الرئيس (من مصدر Tauri وwry)، والحفظ ≤ ٢٦٫٦ م.ث لـ١٦ م.ب.
  - لا مهمة طويلة أثناء الكتابة في ٥٠ ألف حرف.
  - لا نموّ في الذاكرة بعد ١٦٠ تكرارًا.
  - ٢٣ حالة إجهاد × ١٥ دالة بلا انهيار.
  - ٣٠ ردًّا تالفًا من المزوّد، كلٌّ برسالة عربية.
- **أربعة أسئلة للمالك:** في «المشاكل والمعلّقات» أدناه.

## آخر تحقق ناجح

m3، 2026-09-18، على `6c15923`:
- `npm test` **٣٦٢/٣٦٢**، و`isolation.test.js` **٧٦/٧٦**، و`cargo test` **١٧٠/١٧٠**.
- clippy: **٤** للبرنامج و**٩** مع الاختبارات، كما في m1 وm2.
- المتصفح (مرآة 4190، `fresh: true`): U1–U3 بعد الإصلاح، والمهام الطويلة، والذاكرة.
- مراجعة مستقلة (`code-reviewer`، Sonnet): لا حرِج. أُخذ بملاحظتها العالية في «فصل الجمل»، وبملاحظة CRLF.
- لم يُقس بعد: الأرقام الأصلية والمطالبة وزمن المزوّد (m7)، وحجم الحزمة (m8).

## القرارات الحاكمة

القواعد كاملة بأسبابها في `docs/AUDIT-PHASES.md`. أهمّها:
1. مرحلة واحدة ثم توقّف حتى «ابدأ mN».
2. الدليل قبل الحكم: `ملف:سطر` أو مخرج أمر، بصفة **مؤكَّد** أو **من القراءة** أو **[غير محدد - بانتظار التفاصيل]**.
3. القياس قبل الإصلاح، والإصلاح في نطاق المرحلة بحارسٍ كُسِر عمدًا.
4. التصميم يقود: ما يغيّر الشكل أو يضيف سلوكًا يمرّ بـFigma وبقرار المالك.
5. العزل بين نَسَق وشَذْب فوق كل شيء، و`isolation.test.js` أخضر؛ وما يمسّ برومبتًا أو عقدًا يوقف العمل.
6. لا تشغيل للتطبيق إلا في m7، وبإذنٍ في حينه.
7. الوكلاء Sonnet 5 فقط، والحكم في الهوية والتصميم للجلسة الرئيسية.
8. الإيداع على `audit/v27` بمسارات صريحة ثم دفعه. والدمج في `feature/v27-ui` مرةً بعد m8، والحرِج في نهاية مرحلته، من المجلد الرئيسي وشجرته نظيفة. ولا إصدار علني قبله، ولا `main` ولا وسم من الفحص.
9. لا رقمٌ من المتصفح بغير `fresh: true` و`requestFailed: []`.
10. **الاستقلال:** لا قراءة لوثائق مسار البناء ولا لأدواته ولا لذاكرة مراحله، ولا أخذ منها. المصادر هي الكود، وما يقيسه الفحص، وFigma: `DZQACOUgD70llKedxWcHbK`، والصفحات `51:3` و`51:5` و`51:6`.

## المشاكل والمعلّقات

- **الدمج الحرِج لإصلاحات m2 مُرجأ:** شجرة المجلد الرئيسي غير نظيفة (`.claude/launch.json` معدَّل). ويجري حين تنظف، أو مع دمج ما بعد m8.
- **أسئلة المالك من m2** (قرار منتج، لم يُنفَّذ منها شيء، والتفصيل في `m2-data/REPORT.md`):
  1. حفظ النص الجاري واسترجاع الجلسة (m2-16).
  2. تنبيهٌ قبل الإغلاق بصيغٍ غير محفوظة (m2-16).
  3. تأكيدٌ قبل «استعادة إلى المحرر» فوق عملٍ غير محفوظ (m2-14)، وFigma أولًا.
  4. سقف المئة مسودة يحذف الأقدم بصمت (m2-13).
  5. صيغ النص السابق تسقط حين تتغيّر كلماته (m2-15).
  6. إعلام الكاتب حين يُنحّى ملفٌّ تالف، أو يتعذّر تحميل الإعدادات (m2-17).
  7. قفل النسخة الواحدة إن أثبت m7 أن نسختين تعملان معًا (m2-22).
- **أسئلة المالك من m3** (التفصيل في `m3-robustness/REPORT.md`):
  1. «سطور أقل/أكثر» تقول «تعذّر الاتصال بالنموذج» مع أي رفض، ولو كان المفتاح (m3-07).
  2. رسالة الطول «ونسّق كل جزء» تظهر في شَذْب (m3-08).
  3. النص الطويل قد تنتهي مهلته ثم تنتهي كل إعادة: حدٌّ في الواجهة، أم مهلةٌ أطول، أم كما هو (m3-09)؟
  4. إخفاء «إلغاء» بعد اكتمال التنزيل (m3-01)، Figma أولًا.
- **ومن قبل:** التراجع عن «احذف القصّة» (m4)، وإسقاط ملف نصّي في المحرر (m4 وm7).
- **جلستان على شجرة الفحص:** جلسة المالك الأخرى تستعمل المرآة 4186 ومدخلها في `.claude/launch.json`. فالمدخل باقٍ لها غير مودَع، وقياس الفحص على منفذٍ منفصل حين تعمل.

## ملفات الخطوة التالية

- **m4:** `.local/audit/m4-interaction/PLAN.md`، و`LEDGER.md`، و`m3-robustness/REPORT.md` وحده.
- **الكود:** `src/menu.js` و`layout.js` و`shell.js` و`nasaq.js` و`shadhb.js` و`updates.js` (حالته الجديدة: الطلب المؤجَّل و«إلغاء» بلا إعادة تشغيل)، و`src-tauri/src/app/menu.rs`.
- **أداة القياس:** `.playwright-mcp/nsq-runners/README.md` (ومنه فخّ `fill` في m3)؛ ومرآة m3 على 4190 إن شغلت جلسةٌ أخرى 4186.
