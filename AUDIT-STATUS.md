# حالة فحص NSQ

> نقطة الدخول لمسار الفحص: **«ابدأ mN» يبدأ المرحلة N** (m1 … m8). الخريطة والقواعد والبروتوكول في `docs/AUDIT-PHASES.md`، والتفاصيل في `.local/audit/` (محلية).
> يجري الفحص على الفرع `audit/v27` في شجرة عمل مستقلة: `/Volumes/iSltanXx/Projects/NSQ-audit`.
> مسار البناء مستقل: `STATUS.md` و`docs/PHASES.md` على `feature/v27-ui` في المجلد الرئيسي.

**المرحلة الحالية:** m2 — سلامة البيانات والحفظ · لم تبدأ
**آخر مرحلة مكتملة:** m1 — خط الأساس · 2026-09-18
**آخر إيداع للفحص:** إغلاق m1 — وثائق فقط، ولا تعديل في التطبيق
**الفرع:** `audit/v27` — متفرّع من `33af48a` على `feature/v27-ui`، ومدفوع · النسخة `27.0.0`
**سجلّ الملاحظات:** `.local/audit/LEDGER.md` — ٨ صفوف (٢ للتطبيق و٦ لأداة القياس)، و٥ بنود مؤجَّلة إلى m7
**لوحة المتابعة:** لم تُضف مراحل الفحص إليها — سؤالٌ للمالك في تقرير m1.

## آخر ما تم

**2026-09-18 — اكتملت m1.** كل رقمٍ فيها من أوامر الجلسة نفسها لا من `STATUS.md`. و`feature/v27-ui` لم يأتِ بجديد: الفرعان على `33af48a`.

**أداة القياس أُعيد بناؤها في شجرة الفحص، بعد أربعة عيوب وُجدت في أداة المرحلة ٨:**
- **الخادم يُسقط طلبات.** قطع `http.server` طلبًا واحدًا ثم تسعة في ٣٠ تحميلًا، وخادم بطابور ١٢٨ صفرًا وصفرًا. فأي رقمٍ من المرحلة ٨ قد يكون قيس على صفحةٍ نقص منها ملف.
- **ثلاث حالات لم تُقس قط:** خطأ نَسَق، وخطأ شَذْب، و«لا قصّات»، لأن `shell.js:11` يحفظ `invoke` عند الإقلاع. قيست الآن: صفر خرق.
- مشهدان قاسا غير ما سمّياه.
- ثلاث نسخ متباعدة من المحاكاة.

وصار كل قياسٍ يبدأ بفحص طزاجة: بصمة كل ملفٍّ حمّلته الصفحة مقابل الشجرة.

**أول دليلٍ على خطر بيانات يتسلّمه m2 (m1-01، من القراءة):** ملفّ مسودات لا يُقرأ، أو ليس مصفوفة، يُعرض فارغًا، ثم يكتب أول حفظٍ فوقه بلا نسخة.

## آخر تحقق ناجح

m1، 2026-09-18، على `33af48a` + وثائق الفحص:
- `npm test` **١٦١/١٦١**، و`isolation.test.js` **٧٢/٧٢**، و`cargo test` **١٣٠/١٣٠**.
- clippy **٤** تحذيرات للبرنامج و**٩** مع الاختبارات؛ منها واحد جديد بعد المرحلة ٨ (`llm.rs:1668`).
- **صفر** خرق تباين و**صفر** فيضان في ٥٨ قياسًا: ٥ أوضاع × عرضين، و٤٨ تركيبة حالة×مظهر. أطول حركة ٣٠٠ م.ث، و١٥٠ في الحركة المخفّضة.
- منقولة بلا قياس، وتُعاد في m7 وm8: الإقلاع ١٢١٠ م.ث باردًا و٣١٦–٣٢٧ دافئًا، والحزمة ٩٫٩ م.ب.

## القرارات الحاكمة

القواعد كاملة بأسبابها في `docs/AUDIT-PHASES.md`. أهمّها:
1. مرحلة واحدة ثم توقّف حتى «ابدأ mN».
2. الدليل قبل الحكم: `ملف:سطر` أو مخرج أمر، بصفة **مؤكَّد** أو **من القراءة** أو **[غير محدد - بانتظار التفاصيل]**.
3. القياس قبل الإصلاح، والإصلاح في نطاق المرحلة بحارسٍ كُسِر عمدًا.
4. التصميم يقود: ما يغيّر الشكل أو يضيف سلوكًا يمرّ بـFigma وبقرار المالك.
5. العزل بين نَسَق وشَذْب فوق كل شيء، و`isolation.test.js` أخضر؛ وما يمسّ برومبتًا أو عقدًا يوقف العمل.
6. لا تشغيل للتطبيق إلا في m7، وبإذنٍ في حينه.
7. الوكلاء Sonnet 5 فقط، والحكم في الهوية والتصميم للجلسة الرئيسية.
8. الإيداع على `audit/v27` وحده بمسارات صريحة، ثم دفعه. يأخذ آخر `feature/v27-ui` بالدمج قبل كل مرحلة، ولا تعود إصلاحاته إليه إلا بقرار المالك. لا `main` ولا وسم.
9. **لا رقمٌ من المتصفح بغير `fresh: true` و`requestFailed: []`** — من m1.

## المشاكل والمعلّقات

- **للمالك (من m1):** هل تُضاف مراحل الفحص إلى لوحة المتابعة؟
- **موقع الفحص من المرحلة ٩.** التوصية أن يسبق لقطات README والبناء الموقّع والوسم: كل إصلاحٍ بعدها يُبطل اللقطات والبناء. ومعه مصبّ الإصلاحات: تُدمج في `feature/v27-ui` قبل الإصدار، أو تنتظر إصدارًا لاحقًا. قرار المالك، ولا يعطّل m2.
- **قرارات منتج ستُطرح** ولا تُنفَّذ بلا المالك: حفظ النص الجاري واسترجاع الجلسة (m2)، والتراجع عن «احذف القصّة» (m4)، وإسقاط ملف نصّي في المحرر (m4 وm7).
- **منقولة من مسار البناء:** VoiceOver لم يُشغَّل (m7)، و«إلغاء» التنزيل لا يقطع الطلب الجاري (m3)، ورمزان بلا استعمال في `icons.svg` (m8)، وتكرار «الظهور رقم n» بين `shadhb.js` و`prune.js` (m8).

## ملفات الخطوة التالية

m2: `.local/audit/m2-data/PLAN.md` (أُضيف إليه m1-01)، و`LEDGER.md`، و`m1-baseline/REPORT.md` وحده. ثم `src-tauri/src/shared/drafts.rs` و`settings.rs`، و`src/shell.js` (المسودات)، و`src/drafts-model.js`، و`tests/drafts-model.test.js`. وأداة القياس: `.playwright-mcp/nsq-runners/README.md`.
