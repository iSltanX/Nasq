# حالة فحص NSQ

> نقطة الدخول لمسار الفحص: **«ابدأ mN» يبدأ المرحلة N** (m1 … m8). الخريطة والقواعد والمصادر والبروتوكول في `docs/AUDIT-PHASES.md`، والتفاصيل في `.local/audit/` (محلية).
> يجري الفحص على الفرع `audit/v27` في شجرة عمل مستقلة: `/Volumes/iSltanXx/Projects/NSQ-audit`.
> **الفحص مستقلّ عن مراحل البناء:** لا يقرأ وثائقها ولا يأخذ منها شيئًا غير رابط Figma ومعرّفات صفحاته (القاعدة ١٠).

**المرحلة الحالية:** m2 — سلامة البيانات والحفظ · لم تبدأ
**آخر مرحلة مكتملة:** m1 — خط الأساس · 2026-09-18
**آخر إيداع للفحص:** قواعد الاستقلال وقرارات المالك — وثائق فقط، ولا تعديل في التطبيق
**الفرع:** `audit/v27` — متفرّع من `33af48a` على `feature/v27-ui`، ومدفوع
**سجلّ الملاحظات:** `.local/audit/LEDGER.md` — ٨ صفوف (٢ للتطبيق و٦ لأداة القياس)، و٤ بنود مؤجَّلة إلى m7
**لوحة الفحص:** https://claude.ai/artifact/N4RmWpMSpqhNN83jcg5WP6 — مستقلة، وفي لوحة البناء رابطٌ إليها

## آخر ما تم

**2026-09-18 — اكتملت m1، ثم قرّر المالك ثلاثة أمور.**

- **m1 — خط الأساس.** كل رقمٍ من أوامر الجلسة نفسها. وأُعدّت أداة القياس في شجرة الفحص، وأُصلحت فيها أربعة عيوب قبل اعتماد أي رقم:
  - خادمٌ يُسقط ملفات بصمت: طلبٌ ثم تسعة من كل ٣٠ تحميلًا، والخادم الجديد صفر.
  - ثلاث حالات لم تكن تُبلَغ (خطأ نَسَق، وخطأ شَذْب، و«لا قصّات»)، وقيست الآن بلا خرق.
  - مشهدان يقيسان غير ما سمّياه.
  - ثلاث نسخ متباعدة من المحاكاة.

  وصار كل قياسٍ يبدأ بفحص طزاجة.
- **أول خطر بيانات لـ m2 (m1-01، من القراءة):** ملفّ مسودات لا يُقرأ، أو ليس مصفوفة، يُعرض فارغًا، ثم يكتب أول حفظٍ فوقه بلا نسخة.
- **قرارات المالك:**
  1. الفحص مستقلّ عن مراحل البناء، ووثائقها ممنوعة على الفاحص بقواعد رفضٍ في إعدادات الشجرة (القاعدة ١٠).
  2. الإصلاحات تُدمج في `feature/v27-ui` مرةً بعد m8، والحرِج منها في نهاية مرحلته، ولا إصدار علني قبل ذلك (القاعدة ٩).
  3. للفحص لوحته المستقلة.

## آخر تحقق ناجح

m1، 2026-09-18، على `33af48a` مع وثائق الفحص:
- `npm test` **١٦١/١٦١**، و`isolation.test.js` **٧٢/٧٢**، و`cargo test` **١٣٠/١٣٠**.
- clippy: **٤** تحذيرات للبرنامج، و**٩** مع الاختبارات.
- **صفر** خرق تباين و**صفر** فيضان في ٥٨ قياسًا: ٥ أوضاع × عرضين، و٤٨ تركيبة حالة×مظهر. وأطول حركة ٣٠٠ م.ث، و١٥٠ في الحركة المخفّضة.
- لم يُقس بعد: الإقلاع والذاكرة (m7)، وحجم الحزمة (m8).

## القرارات الحاكمة

القواعد كاملة بأسبابها في `docs/AUDIT-PHASES.md`. أهمّها:
1. مرحلة واحدة ثم توقّف حتى «ابدأ mN».
2. الدليل قبل الحكم: `ملف:سطر` أو مخرج أمر، بصفة **مؤكَّد** أو **من القراءة** أو **[غير محدد - بانتظار التفاصيل]**.
3. القياس قبل الإصلاح، والإصلاح في نطاق المرحلة بحارسٍ كُسِر عمدًا.
4. التصميم يقود: ما يغيّر الشكل أو يضيف سلوكًا يمرّ بـFigma وبقرار المالك.
5. العزل بين نَسَق وشَذْب فوق كل شيء، و`isolation.test.js` أخضر؛ وما يمسّ برومبتًا أو عقدًا يوقف العمل.
6. لا تشغيل للتطبيق إلا في m7، وبإذنٍ في حينه.
7. الوكلاء Sonnet 5 فقط، والحكم في الهوية والتصميم للجلسة الرئيسية.
8. الإيداع على `audit/v27` بمسارات صريحة ثم دفعه. والدمج في `feature/v27-ui` مرةً بعد m8، والحرِج في نهاية مرحلته، من المجلد الرئيسي وشجرته نظيفة. ولا إصدار علني قبله، ولا `main` ولا وسم من الفحص.
9. لا رقمٌ من المتصفح بغير `fresh: true` و`requestFailed: []`.
10. **الاستقلال:** لا قراءة لوثائق مسار البناء ولا لأدواته ولا لذاكرة مراحله، ولا أخذ منها. المصادر هي الكود، وما يقيسه الفحص، وFigma: `DZQACOUgD70llKedxWcHbK`، والصفحات `51:3` و`51:5` و`51:6`.

## المشاكل والمعلّقات

- **قرارات منتج ستُطرح** ولا تُنفَّذ بلا المالك:
  - حفظ النص الجاري واسترجاع الجلسة (m2).
  - التراجع عن «احذف القصّة» (m4).
  - إسقاط ملف نصّي في المحرر (m4 وm7).

## ملفات الخطوة التالية

- **m2:** `.local/audit/m2-data/PLAN.md` (أُضيف إليه m1-01)، و`LEDGER.md`، و`m1-baseline/REPORT.md` وحده.
- **الكود:** `src-tauri/src/shared/drafts.rs` و`settings.rs`، و`src/shell.js` (المسودات)، و`src/drafts-model.js`، و`tests/drafts-model.test.js`.
- **أداة القياس:** `.playwright-mcp/nsq-runners/README.md`.
