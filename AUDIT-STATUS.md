# حالة فحص NSQ

> نقطة الدخول لمسار الفحص: **«ابدأ mN» يبدأ المرحلة N** (m1 … m8). الخريطة والقواعد والبروتوكول في `docs/AUDIT-PHASES.md`، والتفاصيل في `.local/audit/` (محلية).
> يجري الفحص على الفرع `audit/v27` في شجرة عمل مستقلة: `/Volumes/iSltanXx/Projects/NSQ-audit`.
> مسار البناء مستقل: `STATUS.md` و`docs/PHASES.md` على `feature/v27-ui` في المجلد الرئيسي.

**المرحلة الحالية:** m1 — خط الأساس · لم تبدأ
**آخر مرحلة مكتملة:** —
**آخر إيداع للفحص:** `3ca7c7b` (الخطة)
**الفرع:** `audit/v27` — متفرّع من `33af48a` على `feature/v27-ui`، ومدفوع · النسخة `27.0.0`
**سجلّ الملاحظات:** `.local/audit/LEDGER.md` — يُنشأ في m1
**لوحة المتابعة:** لم تُضف مراحل الفحص إليها بعد.

## آخر ما تم

**2026-09-18 — وُضعت الخطة.** قائمة فحص المالك (٢٣ بندًا في أربعة محاور، وقواعد عمل) صارت ثماني مراحل m1–m8، والأصل محفوظ كما هو في `.local/audit/SOURCE.md`. قُرئ الكود لربط كل بند بموضعه — **ولم يُنفَّذ فحصٌ بعد**: ما في خطط `.local/audit/` نقاط بداية من القراءة، لا نتائج.

لم يُحذف من القائمة بند: ١٥ كما هي، و٢ بمقابلهما في التطبيق، و٦ كُيّفت لطبيعته. ومن قواعد العمل حُذفت واحدة: «قسّم المشروع بنفسك»، لأن القسمة ثابتة في الخريطة. التفصيل في ملحق `docs/AUDIT-PHASES.md`.

**فرعٌ مستقل بطلب المالك.** `audit/v27` في شجرة عمل `NSQ-audit`، لأن جلسة المرحلة ٩ تعمل في المجلد الرئيسي، وتبديل الفرع هناك كان سينقل إيداعاتها إلى فرع الفحص. وخرجت ملفات الفحص كلها من مجلد البناء.

## آخر تحقق ناجح

لا شيء في مسار الفحص بعد. أساس المرحلة ٨ للمقارنة — يُعاد قياسه في m1 ولا يُعتمد من هنا:
- `npm test` ١٥٣/١٥٣، و`cargo test` ١٢٦/١٢٦، وclippy بثمانية تحذيرات.
- Lighthouse وصولًا 100/100 للنوافذ الثلاث، وصفر خرق تباين وصفر فيضان عند عرضين.
- الإقلاع ١٢١٠ م.ث باردًا و٣١٦–٣٢٧ دافئًا، والحزمة ٩٫٩ م.ب.

## القرارات الحاكمة

القواعد كاملة بأسبابها في `docs/AUDIT-PHASES.md`. أهمّها:
1. مرحلة واحدة ثم توقّف حتى «ابدأ mN».
2. الدليل قبل الحكم: `ملف:سطر` أو مخرج أمر، بصفة **مؤكَّد** أو **من القراءة** أو **[غير محدد - بانتظار التفاصيل]**.
3. القياس قبل الإصلاح، والإصلاح في نطاق المرحلة بحارسٍ كُسِر عمدًا.
4. التصميم يقود: ما يغيّر الشكل أو يضيف سلوكًا يمرّ بـFigma وبقرار المالك.
5. العزل بين نَسَق وشَذْب فوق كل شيء، و`isolation.test.js` أخضر؛ وما يمسّ برومبتًا أو عقدًا يوقف العمل.
6. لا تشغيل للتطبيق إلا في m7، وبإذنٍ في حينه.
7. الوكلاء Sonnet 5 فقط، والحكم في الهوية والتصميم للجلسة الرئيسية.
8. الإيداع على `audit/v27` وحده بمسارات صريحة، ثم دفعه. يأخذ آخر `feature/v27-ui` بالدمج قبل كل مرحلة، ولا تعود إصلاحاته إليه إلا بقرار المالك. لا `main` ولا وسم.

## المشاكل والمعلّقات

- **موقع الفحص من المرحلة ٩.** التوصية أن يسبق لقطات README والبناء الموقّع والوسم: كل إصلاحٍ بعدها يُبطل اللقطات والبناء. ومعه مصبّ الإصلاحات: تُدمج في `feature/v27-ui` قبل الإصدار، أو تنتظر إصدارًا لاحقًا. قرار المالك.
- **قرارات منتج ستُطرح** ولا تُنفَّذ بلا المالك: حفظ النص الجاري واسترجاع الجلسة (m2)، والتراجع عن «احذف القصّة» (m4)، وإسقاط ملف نصّي في المحرر (m4 وm7).
- **منقولة من مسار البناء:** VoiceOver لم يُشغَّل (m7)، و«إلغاء» التنزيل لا يقطع الطلب الجاري (m3)، ورمزان بلا استعمال في `icons.svg` (m8)، وتكرار «الظهور رقم n» بين `shadhb.js` و`prune.js` (m8).

## ملفات الخطوة التالية

m1: دمج آخر `feature/v27-ui` في `audit/v27`، ثم `package.json` (قائمة ملفات الاختبار)، و`src-tauri/`، و`.playwright-mcp/nsq-runners/` في المجلد الرئيسي، و`git log cc4b5d9..HEAD`.
